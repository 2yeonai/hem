---
type: doc
tags: [지침, ai공장]
---
# 카톡자료 인테이크 — 2026-07-18

**원천**: `C:\Users\82106\Documents\카카오톡 받은 파일\KakaoTalk_20260717_2309_33_371_혜미.md` (39,301줄, 2026년 구간 3,089줄~끝)
**처리**: 운영표 §4 인테이크 파이프라인의 "보관" 단계 산출물. Sonnet 서브에이전트 6개가 구간을 나눠 추출, Fable 5가 병합·요약. [gombeck1]
**추출 기준**: ①프로젝트 관련 ②클로드 스킬/설정(설치명령 원문 보존) ③혜미 파악(새 정보만). 잡담·개인 민감사항 원문은 제외.

## 종합 요약 (핵심 발견)

1. **가족 사업 구조가 처음으로 명확해짐** — 온천꽃식물원(창녕 부곡, 대표 이문숙 = 어머니, 등록 608-92-06180), 방역앤클린(진주, 대표 김남오, 등록 404-46-00762 — **혜미 본인 사업 아님을 본인이 명시**), mo,on(혜미 본인의 출장형 산모 회복 컨디셔닝 창업, 예비창업패키지 서류 탈락 이력), 대륙창업(박수길 — **혜미 정정 2026-07-18: 아빠 사업체**). ~~썬홍패치~~ → **혜미 정정 2026-07-18: 혜미 사업 아님, 도원결의 홍재우 강사님의 예시임.** → `2. Areas/핵심맥락.md` 갱신 후보.
2. **RTS의 정체**: 혜미의 재활트레이닝(RTS) 자격 교육 자료. mo,on 서비스 설계의 근거 자격. rts/ 프로젝트(93장 카드)의 원천.
3. **꽃집 최종 사용자는 어머니** — 꽃집 앱/공장의 현장 사용자는 혜미가 아니라 어머니. 지역(창녕/부곡) 공무원·단체 인맥 기반 경조사 주문이 주요 유입.
4. **스킬/설치 후보 다수 확보** (아래 ② 절 참고): 공식(anthropics/skills, playwright MCP)부터 서드파티(ECC, marketingskills, NotebookLM 연동, Graphify, 디자인 스킬 42종, Higgsfield MCP, VFF 등)까지. **VFF는 이전 세션에서 이미 "설치 보류(보안 위험)" 판정**. 서드파티는 `외부 스킬 재평가` 이월 과제와 묶어 선별 설치 필요.
5. **bizinfo API 인증키 발급 기록** (X9HC6x) — 이미 "정부지원 bizinfo 실수집" 커밋과 연결됨.
6. **7/8·7/11 설계 마라톤 기록 확인** — 범용 러너/공통규칙 3가지/신규 9공장 stages/앱형·소수공유 조건 확정 과정의 원문 대화. 볼트에 이미 이식된 내용과 대체로 일치(구현작업_종합자료_0708).
7. **주의**: 방역 골든셋 "정기고객 110곳" 등 수치는 가정치임을 혜미가 명시("내가 하는 사업이 아니라 확실하게 말 못해") — 확정 사실로 취급 금지.

아래는 구간별 추출 원문이다.

# 구간 1 (3089~9124줄)

## ① 프로젝트 관련

- `[7305~7612줄 부근, 2026-03-07~03-21]` **mo,on(모온) 예비창업패키지 사업계획서** 작업 — 혜미가 직접 준비 중인 정부지원사업(예비창업패키지) 신청. 파일 다수 업로드: `moon pricelist.pdf/v2/hq`, `mo on 예비창업패키지 사업계획서 3월8일 pm 8시.pdf`, `moon 사업계획서 v4.docx`, `[별첨_1]_2026년_예비창업패키지_사업계획서_초안 v1~v2.hwp/docx/pdf`, `예창패 초안 3~4.pdf`, `예창패 제출용 근거 리서치 검증 정리서.docx`.
- `[7328~7395줄]` 사업계획서 근거자료로 쓰인 통계: 보건복지부 「2024년 산후조리 실태조사」(산후조리원 이용 85.5%, 본인집 84.2%, 산후조리원 평균 이용 12.6일/전체 30.7일), 행정안전부 주민등록 인구통계 기준 진주시 출생등록(2023 1,693명→2024 1,705명→2025 1,864명 증가). 서비스 컨셉: "출장형 산모 회복 관리 서비스", 기존 마사지 중심 산후관리 대비 "체형 분석 기반 회복 프로그램 + 회차별 데이터 축적"으로 차별화.
- `[7408줄]` MVP 계획: 초기엔 노션·구글폼으로 운영 → 이후 고객 데이터 입력/조회/회차 기록/관리자 페이지가 있는 웹 입력폼을 외주 개발(예산 약 600만원으로 산정).
- `[7423~7440줄, 2026-03-23]` 추가 자료: `정부지원사업 합격예시 2/3.pdf`, `보헤 예창패.pdf`, `자료 6. 체형분석 평가지 2~8종.xlsx/pdf`, `bohe_inventory_120.csv` (재고 데이터로 보임, "보헤"는 다른 브랜드/상호명일 가능성).
- `[7441~7611줄]` Claude(추정)에게 사업계획서 삽입용 인포그래픽 2건 제작 요청 — ① "mo,on(모온) 4단계 회복 프로그램 흐름"(1순환회복관리→2골반안정화관리→3체형회복관리→4생활관리교육, 저채도 그린+네이비+그레이 톤, 인스타 감성 금지·공공지원사업 제출용 톤 강조), ② "협약기간 내 추진 일정"(2026.06~2027.01 미니 간트차트, 5개 행: 프로그램 설계/파일럿 운영/시범 운영/매뉴얼 보완/채널 정착). 세부 디자인 지시사항(금지사항 포함)이 상세히 명시되어 있음 — 추후 인포그래픽 재요청 시 참고 가능.
- `[7614~7940줄, 2026-03-24]` **산모 특화 체형분석 기록양식 5종** 상세 스펙 — A4 1장, 맑은고딕, 의료기록지 스타일, 인체 실루엣 포함. 양식1: 산후 정적 정렬 및 체형 분석 평가표(측면/전후면 관찰, Plum Line, 복직근이개 DR 간격). 양식2: 견관절·경추·체간 ROM(관절가동범위) 평가표(경추/흉요추/견관절 각도 기준치 명시). 문서 하단에 "본 평가는 의료적 진단이 아닌 회복 관리 목적" 비의료 면책 문구 반복 사용. (양식 3~5는 이번 구간에서 끝까지 확인 못함, 뒤쪽 구간에 이어질 가능성 있음)
- `[7319, 7323줄]` `RTS 완성교재 v5.pdf`, `RTS LV 1.pdf` 업로드 — CLAUDE.md/git 로그에 언급된 "RTS 93장" 자료의 출처로 추정됨 (RTS = Rehabilitation Training Specialist, 혜미의 자격증/서비스 설계 근거).
- `[7359~7362줄]` ChatGPT 커스텀 GPT 링크 공유: `독고다희_2급생체_필기_전용_오답노트` — 다른 자격증(2급 생활스포츠지도사 추정) 공부용 GPT로 보임, `독고다희_2급생체_핵심요약집(Ver2.0).pdf` 첨부.
- `[7429줄]` 애플 릴레이 이메일 `k9cjhmw7z9@privaterelay.appleid.com` 노출 — 이 문자열은 git 커밋 로그에서 보이는 계정 태그 `[k9cjhmw7z9]`와 동일함. mo,on 사업계획서 자료 업로드 시점(2026-03-23) 근처에 등장 — 해당 계정 태그가 이 시기 세션(아마 다른 기기/계정)에서 쓰인 것과 연결되는 단서.

이 구간의 나머지 대부분(약 3399~5905줄, 5906~7304줄, 7940~9124줄)은 혜미의 **개인 부업(출장 산후마사지) 계약 해지 분쟁**(내용증명 초안, 계약서 전문, 증거목록, 대면 녹취 전사문 등)으로 채워져 있음 — AI 공장/옵시디언 볼트 프로젝트와 무관하여 상세 추출 대상에서 제외함(③에 시점만 간단히 기록).

## ② 클로드 스킬/설정

없음 — 이 구간(3089~9124줄)에는 Claude Code 스킬, MCP, 커넥터, 설치 명령어 관련 내용이 없음. (설치 관련 매치는 모두 가전제품 설치/AS 얘기였고, 실제 개발/설치 명령어 블록은 이 구간 밖인 16000줄 이후부터 등장함)

## ③ 혜미 파악

- `[3399~9124줄 전반]` 2026년 2월~4월 사이, 출장 산후마사지 부업 관련 계약(원장 김지선, 2025.5.26 체결·7년·교육비 500만원) 해지를 둘러싼 개인 법적 분쟁에 상당한 시간·에너지를 씀(내용증명 발송, 노동청 진정/민사소송 검토, 정산 협의). AI 공장 작업과 직접 관련은 없지만, 해당 시기 혜미의 가용 시간·심리적 여력에 영향을 줄 수 있는 배경으로 참고할 만함. (세부 분쟁 내용은 개인정보/법적 민감사항이라 이 문서에는 옮기지 않음)
- `[7305~7940줄]` 위 분쟁과 별개로(혹은 그 이후 진로 전환으로) 혜미는 **"mo,on(모온)"**이라는 자체 브랜드로 진주시 기반 출장형 산모 회복관리 서비스 창업을 준비 중이며, 2026년 3월 정부 "예비창업패키지"에 지원함. RTS(Rehabilitation Training Specialist) 자격/교육 이수를 서비스 설계 근거로 활용하고, 체형분석 기록양식·회차별 데이터 축적을 통한 "데이터 기반 컨디셔닝 서비스"를 차별점으로 내세움. 협약기간 목표는 2026.06~2027.01.
- 사업 소재지/타깃 지역은 **진주시**로 확인됨(출생등록 통계를 진주시 기준으로 인용).
- 혜미는 사업계획서에 들어갈 인포그래픽·기록양식 같은 문서 산출물을 만들 때 "공공지원사업 제출서류다운 단정한 톤", "인스타 감성 디자인 금지", "장식·아이콘 남발 금지"를 명확히 선호/지시하는 경향이 있음 — 향후 정부지원사업/사업계획서용 디자인 작업 시 참고할 만한 톤앤매너 기준.

---

**보고**: ① 프로젝트 관련 9건 / ② 클로드 스킬·설정 0건(없음) / ③ 혜미 파악 4건

---

# 구간 2 (9125~15160줄)

## ① 프로젝트 관련

- (약 9450~10826줄) **RTS 재활트레이닝 레벨2 수업노트 정리 작업.** 4/7·4/8 수업 녹음(m4a)을 전사문 기반으로 Claude가 학습노트로 정리(가동성 평가, 어깨검사 5종, MET/ART/MFR 테크닉, 베이튼 스코어 등). 동일 내용이 중복 붙여넣기됨(포맷 다른 버전 2회). 이후(10828~10876줄) 4/10~4/30에 걸쳐 RTS 관련 파일 다수 전달됨: 교재 통합본.pdf, RTS_통합노트 요약본.docx, RTS_3월_근육정리표_전체.xlsx, 모의고사·오답노트·시험직전 요점본 등. ChatGPT 프로젝트 링크도 공유됨: `https://chatgpt.com/g/g-69dcadb31a788191a2d1fa79772b83ff-rts-gom` (RTS 전용 GPT). 로컬 경로 언급: `C:/Users/82106/Desktop/rts/transcripts/수업노트/교재 통합본.pdf` — RTS 관련 작업 폴더가 바탕화면에 별도로 존재함(볼트 밖 위치). 최근 커밋 로그의 "RTS 93장 완성"과 직결되는 원천 자료.

- (10880~10935줄) **예비창업패키지(여성) 지원서 — "mo,on(모온)" 산모 회복 컨디셔닝 서비스.** 지원서 초안 여러 버전(Q1~Q5) 작성 과정. 핵심: 산후조리원 퇴소 2주~3개월 산모 대상, 회복 사진을 AI(ChatGPT 4o + 자세인식)로 분석해 회복단계 분류(기존 기준과 약 82% 일치 확인), 4단계 프로그램(순환회복/골반안정화/체형회복/생활관리교육) + 5종 기록체계(골반균형·복부코어·부종순환·통증불편·수면상태)로 관리. 초기 MVP는 카카오톡 채널+노션. 진주 산모 30명 베타테스트 계획.
  - (11397~11408줄) **서류평가 결과: 탈락(미흡) 통보** — 한국여성벤처협회 전략사업본부. 사유: 기존 산후조리 서비스와 차별화 부족, 시장성/실행전략 미흡, 독보적 경쟁우위 근거 부족.

- (11041~11524줄) **온천꽃식물원 — "창녕 안심이동 생태 테라리움 기프트" 생활문화혁신지원사업.** 부곡온천(창녕군) 소재 온천꽃식물원 명의로 지원. 핵심 아이템: 지역 캐릭터 '따오'(따오기 모티브) + 5중 이동안정 패키지 구조(입구 고정링/측면 완충패드/둥지형 받침대/바닥 충격흡수판/미끄럼방지패드)로 테라리움 이동파손·누수 문제 해결. 관광객 휴대·기관납품·온라인배송 확장 계획. 총사업비 1억 1,120만원(정부지원 1억+자부담 1,120만). Claude와 함께 패키지 구조 설계(따오기 모티브 손잡이 잠금구조), Q&A 예상질문지, 발표대본, 심사위원 압박모드 대본까지 상세 작성.
  - (11550~12698줄) **"따오 발표 연습기" HTML 웹앱 자체 제작** — 슬라이드별 대본 텔레프롬프터, TTS(브라우저 음성합성), 녹음 기능, 일반/압박 심사위원 모드 Q&A 랜덤 출제(21문항, 그 중 압박형 12문항) 기능을 가진 발표 연습용 단일 HTML 파일. 배포 링크: `https://2yeonai.github.io/presentation-practice/` (GitHub Pages).
  - (12799~12871줄) 대면발표 평가지표표(창의성·도전성 20점, 개발과정·방법 40점, 목표시장·사업화 40점 등) 공유됨.

- (13034~15160줄, 매우 중요) **"혜미의 정부지원사업 작업 방식" 방법론 정리 + "혜미식 AI 작업 공장" 설계 프롬프트.** 볼트 CLAUDE.md의 "AI 공장" 패턴 및 `1. Projects/클로드 정부지원사업 ai/jbjw-main/jbjw-main/hyemi-ai-factory/`(별도 프로젝트로 문서화된 폴더)의 원류로 보이는 대화.
  - 10단계~11단계 방법론: 사업분리(사업자별로 아이템 안 섞기) → 자격/결격 위험점검 → 과제명 LOCK → 고객문제정의 → 기존대안 한계 → 솔루션 기능모듈화 → PSST(Problem-Solution-Scaleup-Team) 구조 → 문단→이미지→표 순서 → 증빙 패키지화 → 예산-산출물 연결 → 위험표현 제거(예: "완전자동화"→"대표자 검수 후 처리", "구독료"→"AI API 연계 및 개발비") → 단계별 LOCK.
  - **가족 사업 구조가 명시적으로 드러남**: "온천꽃식물원, 방역앤클린, mo,on, 썬홍패치 같은 아이템을 한 채팅 안에서 섞지 않고 따로 본다", "엄마 사업인지, 아빠 사업인지, 내 사업인지 확정" — 즉 온천꽃식물원(엄마 이문숙 명의 추정), 방역앤클린(아빠 사업으로 추정), mo,on(혜미 본인 창업), 썬홍패치(별도 신규 아이템, 저자극 3중구조 UV차단패치)가 모두 혜미가 문서작업을 지원하는 별도 사업체임.
  - (13781줄~) **"혜미식 AI 작업 공장" 설계 지시 프롬프트** — 반복 산출물 생산 시스템을 목표로 함. 5개 생산라인 설계: A.정부지원사업 공장(공고분석→아이디어LOCK→PSST본문→예산표→증빙연결→위험표현제거→최종검수), B.발표자료 공장, C.발표연습기 공장, D.콘텐츠 변환 공장(블로그/인스타/릴스/카드뉴스/썸네일/스레드), E.업무자동화 아이템 설계 공장(라벨PDF·배송체크리스트 등). 4차 공정(전체설계→프로젝트파일/워크플로우 생성→MVP테스트→심사위원모드 검수)까지 요청. 프로젝트 폴더구조 `hyemi-ai-factory/00_rules ~ 10_handoff` 지정 — CLAUDE.md에 이미 기록된 "jbjw-main/hyemi-ai-factory" 폴더와 동일 구조로, 이 대화가 그 프로젝트의 최초 설계 프롬프트로 추정됨.

## ② 클로드 스킬/설정

- 없음 (이 구간에는 설치 명령어나 MCP/커넥터 설정 관련 내용 없음)

## ③ 혜미 파악

- **가족 사업 구조 확인 (신규, 중요)**: 혜미는 본인 창업(mo,on, 산후회복 컨디셔닝)뿐 아니라 부모님 사업의 AI/문서 작업도 대행함.
  - **온천꽃식물원**(부곡온천, 창녕군) — 대표자 이문숙(발표대본 상 "온천꽃식물원 대표 이문숙"으로 명시, 2000년부터 화훼현장 참여·2015년부터 직접 운영, 50대 후반). 혜미는 딸로서 지원사업 신청 전반을 돕고 있음("딸이 전폭적으로 돕고 있고"). "1. Projects/클로드 꽃집 ai" 팩토리의 실제 사업 주체로 추정됨.
  - **방역앤클린** — "아빠 사업"으로 언급됨 (13246, 13464줄). "1. Projects/클로드 방역 ai" 팩토리의 실제 사업 주체로 추정.
  - ~~**썬홍패치** — 신규 언급된 아이템~~ → **❌ 혜미 정정(2026-07-18): 혜미 사업 아님, 도원결의 홍재우 강사님의 예시임.**
  - **mo,on** — 혜미 본인의 산후 회복 컨디셔닝 창업 아이템(위 ①에 상세). 미용사(피부) 국가자격증 보유(2025-10-21~2026-01-28 교육 이수), RTS 재활트레이닝 교육 이수 중.

- **육아휴직 중 + 최근 이직/구직 상태 관련 정보 노출**: 6/10줄 카톡 문자에 "[취업] 창녕군시설관리공단(2017-01-03~2026-04-02)" "[교육·직업훈련] 씨티우먼뷰티아카데미학원 미용사(피부)국가자격증과정" 이력 확인 메시지 수신 — 청년센터 관련 지원사업(예: 국민취업지원제도 등) 신청 관련으로 추정.

- **진행 중인 노동/계약 분쟁 (맘케어 김지선 원장 건, 9125~9448줄 및 11060~11141줄)**: 2025년 5월 출장 산후마사지 업체 "맘케어"와 교육계약(교육비 500만원) 체결 후 계약 해지를 둘러싼 정산 분쟁. 내용증명 1차(2/19)·2차(3/4) 발송, 이후 고용노동부(진주지청) 임금체불 진정 제기 — 근로자성 불인정으로 진행 중(6/1자 근로감독관 답변). 상대측(김지선 원장)이 6/1 카톡에서 혜미의 육아휴직·한부모 관련 개인정보를 언급하며 위협성 메시지를 보냄 — 다만 이는 분쟁 상대방의 일방적 주장이며 사실관계 미확인 상태이므로 그대로 신뢰할 근거는 아님. 참고용으로만 기록.

- **AI 활용 업무 스타일 재확인**: 정부지원사업 서류작업 시 "무조건 긍정 금지", "심사위원이 감점할 표현부터 제거", "단계별 LOCK" 등 매우 체계적·방어적인 접근을 선호. 이는 이미 알려진 "비개발자 AI 공장 설계자" 특성과 일치하되, 방법론이 이번 구간에서 처음으로 명시적 텍스트로 상세히 드러남.

## 출력물 요약 위치 (참고용 원본 발췌 파일 없음, 본 요약 파일이 유일 산출물)

---

# 구간 3 (15161~21196줄)

## ① 프로젝트 관련

- (15167~16867줄) 정부지원사업 지원서 작성 17단계(0~16단계) 방법론 전문 + "최종 실행 지시" 프롬프트(1~4차 공정: 공장설계→파일/규칙 생성→MVP 실행테스트→심사위원 검수, 토큰 부족 시 CHECKPOINT/HANDOFF.md 형식으로 안전 중단). 클로드 정부지원사업 ai 스킬(gov-support-matching-skill)의 원본 작업절차서로 보임 — 현재 SKILL.md/manifest.yaml에 이 17단계가 전부 반영됐는지 대조 필요.
- (16868~19600대) 온천꽃식물원(꽃집) 주문관리 앱 "Oncheon Flower AI Order Center" 실제 개발 기록. Next.js + Cloudflare Workers/Pages + D1(DB명 delivery-db) 스택. 통화 STT→주문 자동추출, 리본 문구를 ribbonSenderText/ribbonMessageText/ribbonType으로 분리, 수령자명/기관명/부서명 분리 추출(보내는사람을 "온천꽃식물원"으로 잘못 기본값 처리하는 버그 다수 수정 지시), 배송완료 사진 촬영→재촬영/사용선택→문자발송, 고객이력·중복주문 감지, 통계/설정 화면 구현. 공개 배포: https://oncheon-flower-ai-order.pages.dev (Pages는 배포됨, Worker는 D1 미생성으로 실배포 보류 상태였음).
- (18400~18872줄) 꽃집 앱 관련 후속 정밀 지시서(코덱스용): 권한설정 마법사, 통화녹음 파일 "가져오기"(직접 녹음 생성은 금지), 로컬 파서로 기관명+사람명 분리, 리본 문구 정규화 사전(승진축하→승진을 축하드립니다 등), 금액 표현 정규화(십만원/오마넌 등→숫자), STT 한국어 오인식 보정("백"↔100 등 성씨 오인식 사전), 저장/확정/출력 단계별 필수값 분리, 모바일 레이아웃(iOS safe-area 등) 수정, 배송사진 플로우 상세 스펙.
- (17909~17938줄) 사고 기록: 꽃집(delivery-app) 관련 커밋 107개(+7,180줄)가 실수로 "정부지원사업 지원앱(hyemi-ai-factory)" 저장소(GitHub `2yeonai/jbjw`)에 들어갔다가 전량 원복(커밋 2273b9d)됨. 볼트 CLAUDE.md에 이미 기록된 "꽃집 폴더=정부지원 스킬 사본" 혼선과 유사한, 실제 코드 저장소 레벨의 프로젝트 혼선 실사례.
- (19104~19597줄) hyemi-ai-factory(정부지원사업 AI 공장) 로컬 실행 트러블슈팅 전 과정 기록: 저장소 `2yeonai/jbjw`, 작업 브랜치 `claude/grant-application-factory-3coh2g`, 로컬 실행은 `python 08_factory_tools\app.py`. 서버 실행 시 "이 PC / 휴대폰(같은 와이파이)" 접속 주소가 자동 출력되도록 개선됨. 클로드가 "푸시 완료"라 보고한 커밋(10e0fc3)이 실제 GitHub에는 없어 미푸시 상태였던 신뢰성 문제 발생 → git add/commit/push, git pull --rebase로 복구.
- (19928~19933줄) hyemi-ai-factory 실전 적용 프로젝트 "2026_ai_support"(2026 혁신 소상공인 AI 활용지원 공고) 진행상황: 0단계(신청자 정보) 완료, 신청자·아이디어 입력 대기 중 — 다음 세션 이어갈 지점.
- (19614~19678줄) 정부지원사업 스킬(gov-support-matching-skill) v0.3.0 업그레이드 지시: 기존 confidence_passed + judge_pass_recommendation 이중 게이트에 judge_quality_score(6개 세부점수: 문제구체성/증빙강도/예산타당성/AI필요성/실행가능성/위험언어, 0.85 이상 통과) 추가. READY_FOR_APPROVAL = confidence_passed ∧ judge_pass_recommendation ∧ judge_quality_score≥0.85 ∧ 확인필요 0개 ∧ 필수서류 통과. Cowork/Codex에 위임 예정, 작업 전 bundle 백업 필수 지시.
- (20087~21160줄) Fable 5 세션에서 나온 "신규 후보 공장" 전략 논의: 아이디어 공장, 학습변환 공장(강의 STT→체크리스트/PPT/카드뉴스), 가구배치/공간배치 공장, 냉장고·집밥 매니저 공장("나만의 냉장고" 상세 기획서, 20087~20086줄 — 식재료 소진기한 기반 식단표+장보기 자동생성, 아기식단 분기 포함), 옛날옛적에(AI 키즈 동화·동요·영상 공장, 20399~21029줄 상세). 최신 우선순위: 아이디어공장 > 학습변환공장 > PPT공장 > 옛날옛적에 > 냉장고공장 > 가구배치공장 > 챗봇/AI직원 > 이미지/AI인간 > 콘텐츠재생산 > 3D캐릭터/굿즈 > RAG공장. Fable에게는 "코드/UI/실행 금지, 설계(purpose/io_contract/pipeline/quality_gate/human_review/lower_model_handoff/blind_spots/stop_rules/48시간계획)만" 요청하는 방식으로 진행하기로 함(21030~21160 "아이디어 공장", "학습변환 공장" 설계 초안 시작).
- (19602~19610줄) 혜미가 공유용으로 다듬어온 에이전트 지침 문서(AGENTS.md) 공개 저장소: https://github.com/jeiel85/AGENTS.md — 오푸스/소넷도 페이블 수준 추론을 하도록 강제하는 시도의 산출물.

## ② 클로드 스킬/설정

- (16888~17089줄) "Lead Software Architect / Senior Full-Stack Engineer / Product Owner / AI Systems Engineer" 역할을 합친 `@agent` 프롬프트 템플릿 — 혜미가 직접 개선한, 장기 프로젝트 일관성을 위한 프로젝트 헌장형 프롬프트 기법(코드 조직 규칙, /docs 문서 목록, AI 원칙, 도메인 규칙 포함).

Cloudflare Worker 배포 절차 (원문 그대로):
```bash
cd delivery-app/worker
npx wrangler login                    # ① Cloudflare 계정 인증 (브라우저 OAuth)
npx wrangler d1 create delivery-db    # ② 출력된 database_id를 wrangler.toml에 기입
npm run migrate:prod && npm run deploy
./scripts/smoke.sh <Worker URL>       # 배포 검증 (9개 전부 PASS 확인)
```

SMS 실발송 활성화 (원문 그대로):
```bash
wrangler secret put SMS_API_KEY
```

Capacitor 패키지 설치 (원문 그대로):
```bash
npm.cmd install @capacitor/core @capacitor/android @capacitor/cli
```

JDK 설치 시도, winget (원문 그대로):
```
winget install --id EclipseAdoptium.Temurin.17.JDK --silent --accept-package-agreements --accept-source-agreements
```

Git 설치, winget (원문 그대로):
```
winget install --id Git.Git -e
```

저장소 클론 후 정부지원사업 AI 공장 로컬 실행 (원문 그대로):
```
cd Desktop
git clone https://github.com/2yeonai/jbjw.git
cd jbjw
python 08_factory_tools\app.py
```

이미 클론된 상태에서 갱신 후 실행 (원문 그대로):
```
git pull
python3 08_factory_tools/app.py
```

미커밋 작업 저장 (원문 그대로):
```
git add .
git commit -m "save current hyemi grant factory work"
git push
```

push 실패 시 (원문 그대로):
```
git push origin claude/grant-application-factory-3coh2g
```

원격 변경사항 먼저 반영 (원문 그대로):
```
git pull --rebase origin claude/grant-application-factory-3coh2g
```

작업 안전 백업 (원문 그대로):
```
git branch backup-hyemi
```

커밋 실존 여부 확인 (원문 그대로):
```
git branch -a
git log --all --oneline | findstr 10e0fc3
```

Cloudflare 인증 상태 확인 (원문 그대로):
```
npx wrangler whoami
```

## ③ 혜미 파악

- 온천꽃식물원 꽃집의 실제 사용자는 혜미 본인이 아니라 **어머니**로 보임("엄마 폰에 옮겨 설치할 때 그대로 쓰면 됩니다", "엄마가 앱 설치 후 처음 한 번만 안내에 따라 권한을 허용하면"). 즉 꽃집 AI 공장의 최종 현장 사용자는 혜미 어머니.
- 혜미는 같은 개발 요청을 Claude Code뿐 아니라 **코덱스(Codex)**, **지피티(GPT)**에도 동시에 보내 비교/이어받기 하는 멀티 에이전트 운용 습관이 뚜렷함(예: 17909줄 "코텍스 작업 @agent", 18935줄 "지피티 확인", 19558줄 "지피티" — 동일 프롬프트를 여러 도구에 반복 전송).
- 개발 지시 프롬프트를 매우 길고 구조화된 형태(번호 매긴 요구사항 + 금지사항 목록 + "완료 보고 형식" 항목별 사전 지정)로 작성하는 경향이 강함 — 특히 꽃집 앱 지시에서 두드러짐.
- "개발자 아닌사람이 알아들을 수 있게 해ㅈ줘"라고 명시적으로 요구한 실제 발화 확인(19509줄) — CLAUDE.md의 비개발자 커뮤니케이션 규칙이 실제 대화에서도 반복 요구됨.
- 프로젝트 혼선에 민감함: 꽃집 코드가 정부지원사업 저장소에 잘못 들어간 것을 발견하자 "이 저장소는 정부지원사업 지원앱(hyemi-ai-factory)이고, 꽃집 배송/APK 관련 변경을 전부 제거합니다"라고 즉시 원복을 지시·집행함(19909줄).
- PC에 `.claude`, `.codex`, `.gemini`, `.antigravity`, `.verdent`, `.n8n`, `.agents`, `.android` 등 다수 AI 코딩 툴 설정 폴더를 동시에 보유(19059~19073줄 dir 결과) — 여러 AI 코딩 툴을 동시에 실험/운용 중.
- (16869~16870줄) "예술인경력정보시스템" 계정 비밀번호 초기화 문자(수신자명 "정혜원")와 평문으로 보이는 비밀번호 문자열이 카톡에 그대로 붙여넣어져 있음 — 혜미(또는 관계자)의 타 계정 활동 정황. 비밀번호 원문은 민감정보라 본 요약에는 옮기지 않음; 볼트 원본 파일 자체에 노출돼 있으니 필요시 마스킹 검토.

---

# 구간 4 (21197~27232줄)

## ① 프로젝트 관련

- (21197~22349) **신규 공장 후보 9종 상세 기획서**: 2.학습변환(강의영상→요약/실행체크리스트), 3.PPT공장, 4.챗봇·AI직원 공장, 5.이미지·AI인간 공장, 6.3D캐릭터·IP·굿즈 공장, 7.가구배치·공간배치·렌더링 공장, 8.냉장고·집밥 매니저 공장, 9.콘텐츠 재생산 공장, 10.옛날옛적에(AI 키즈 동화·동요·영상 공장). 각각 한줄정의/철학/문제/사용자/시나리오/입출력/MVP/장기확장/quality_gate/Fable5가 판단할 것/하위모델이 할 것을 구조화해서 기술.
- (22349~22381) **전체 신규 공장 우선순위 확정**: 1아이디어공장 2학습변환 3PPT 4옛날옛적에(키즈동화) 5냉장고·집밥 6가구배치 7챗봇·AI직원 8이미지·AI인간 9콘텐츠재생산 10.3D캐릭터·굿즈. "앱 하나가 아니라 입력→판단→생성→검수→저장→재사용→확장까지 가진 공장으로 설명해야 한다"는 핵심 원칙 명시.
- (22383~23048) **Fable 5 운영 지침 확정**: raw 문서를 팩트체크(adaptive thinking 상시on 검증됨, 토큰 +30~35% 검증됨, 가격표 검증됨/9번 비용시나리오는 추정치)한 뒤 CLAUDE.md/AGENTS.md에 바로 넣을 최종 라우팅 정책(영문) 작성 — Model Roles 표, Never/Route to Fable 5 조건, 호출 전 4단계 게이트, 카드 스키마, Fallback Protocol(Fable 미가용시 Sonnet/Opus를 승격뇌처럼 강제하는 2-pass·adversarial 규칙).
- (23048~23196) 위 정책을 실제 "클로드 정부지원사업 ai" 폴더 CLAUDE.md/SKILL.md에 추가 지시 — 기존 예산정합성/배점매칭/eligibility 내용은 건드리지 않고 Fallback Protocol 섹션만 추가. 라우팅 게이트 0단계(4개 유형 미해당시 즉시 종료) + "상위모델 승격 전 반드시 혜미에게 먼저 확인" 규칙 신설.
- (23204~23206) 혜미가 과거 ChatGPT 팀사이트로 "온천꽃식물원" 주문 앱을 만들어봤으나(`oncheon-flower-ai-order.workspace-623337.chatgpt-team.site`) API 없이 만들어서 정확도가 낮았다고 언급 — 꽃집 프로젝트의 이전 시도 기록.
- (23224~23394) **AI Factory 통합 기획서 v1** (Fable5가 작성, 2026-07-07 기준): 최종형태=모바일앱, 우선순위 ⑪아이디어공장→⑥자료정리·개인지식→②옛날옛적에→③가구배치 순. 16주 타임라인, 공장별 MVP/보류범위/검증방법/수익화순서, 공통인프라(입출력계약/저장구조/검수게이트/버전로그/모델라우팅), 리스크 요약, 이번주 할일 5개.
- (23395~23657) **하위 모델 프롬프트 팩 v1**: 정적 프리픽스(카드스키마·승격규칙·금지사항) + Haiku급(태그추출/분류/얕은요약/중복판정) + Sonnet·Opus급(카드병합/재랭킹/초안작성/승격게이트) + 실행기(Codex/Claude Code) 프롬프트 템플릿 전체, 조립 예시, 운영체크 3가지(승격률 모니터링/confidence 인플레 감시/스키마 드리프트).
- (23660~23795) **4개 프로젝트 진행현황 정리(2026-07-07)**: ①정부지원사업(완성 90%) ②꽃집(설계 거의 완료 — 골든셋 6건 수집, 정부지원 로직 재사용률 0~15%만 확인, 12봇→14봇 확장(주문판정봇·주문분리봇 신규 추가), bundle_id/bundle_sequence/bundle_status로 통화 1건 다중주문 처리 구조 설계 완료) ③방역(착수 예정) ④AI회사 플랫폼(미착수). 다음 할일: 꽃집 설계 산출물을 방역에 얼마나 재사용 가능한지 Cowork에 확인 위임.
- (23820~23877) **방역 스킬 구조 확정 지시**: manifest.schema.v2.yaml에 `triggers`(event/schedule 방식 시작지점, 스케줄러는 스키마 밖 별도 소형 프로그램으로 분리) + 각 stage `run_if`(조건부 실행) 항목 신설 지시. 방역 manifest 초안을 "클로드 방역 ai" 폴더에 신규 생성, 꽃집 폴더는 건드리지 않음. 아직 정하지 않아도 되는 3항목(다중 방문지 분리봇/현장사진첨부봇/견적산정봇)은 run_if로 자리만 만들어둠.
- (23878~24157) **Fable 5 승격 아키텍처 결정 (2건)**: (1) "AI 공장" 전체를 관통하는 employee_io_contract 표준 양식 확정 — 필드: id/kind(local·model·human)/input/output/run_if/check/on_fail/escalation/stop_rule/tier/async. composition_rules(순차/분기/병합/인간게이트필수/trigger로만 시작). platform vs domain 경계 정의. ①②③를 골격에 얹는 5단계 계획, blind_spots(공유장부 스키마 부재, human 대기중 상태 재개 방식 부재, 계약서 버전관리 부재 등), stop_rules. (2) 11개 공장 전체를 아우르는 **공용 AI직원 카탈로그** 확정 — 14개 역할(수집원/변환원/분류원/해석원/요약원/구조설계원/초안작성원/규칙판정원/검수원/사람검수자/포장원/질의응답원/기록원) 정의 + 공장별 조합표(factory_compositions) + 고재사용 직원 vs 공장전용 불가피 직원 구분.
- (24158~24181) 위 카탈로그에 대한 Fable5 최종 판정: 챗봇은 예외가 아니라 **골격을 파이프라인형/상주형(서비스형) 두 종류로 공식 분리**해야 함(냉장고·자료정리도 상주형 후보). kind에 `tool(external)` 신규 추가(외부 이미지도구·영상편집기 통합 흡수). 동화 인간게이트는 아동 직접전달이면 자동검수만으로 대체 불가 — 배치승인 또는 사전승인 템플릿풀 절충안 제시. 공유장부는 3개 공장(자료정리·챗봇·냉장고) 착수 전 최소 스키마(id/출처/유형/본문참조/태그/생성시각/생성공장/상태) 확정이 선행조건.
- (24182~24220) HANDOFF.md(정부지원/꽃집) 및 정부지원사업 신청 관련 PDF 다수 첨부(최종제출본/5장제출본/사업계획서/발표대본/QnA/보조자료/전자책/공고문/신청서식 등), 구글드라이브 링크, 사진 다수(7/8).
- (24201~24210) ~~혜미가 맥미니 구입 후 작업환경 구축, teamclaude로 클로드 다중 계정 순환 셋업~~ → **❌ 혜미 정정(2026-07-18): 스레드에서 퍼온 글을 잘못 추출한 것, 혜미 얘기 아님** (같은 구간 ③의 정정 참고).
- (27055~27072) 혜미가 지시한 Obsidian PARA+Zettelkasten 폴더구조 원문(1.Projects~7.Attachments, 5.Zettelkasten 하위 00.Inbox/10.Literature/20.Permanent) — 각 폴더에 claude.md로 목적·규칙 기재 지시. 현재 프로젝트 CLAUDE.md의 구조 설명과 동일 출처로 보임.
- (27073~27232, 구간 끝까지 이어짐/문서 중간에 잘림) Fable5 무료기간(7/12) 마감 전 세팅 가이드 — "세팅01: 나에게 맞춘 개인지침(사용자레벨 CLAUDE.md) 생성 프롬프트", "세팅02: Fable의 사고방식을 프로토콜로 옮겨 프로젝트 지침에 넣기" 프롬프트(진행 중 구간에서 파일 끝남, 이어지는 세팅 03~05는 다음 구간에 있을 가능성).

## ② 클로드 스킬/설정

아래는 혜미가 카톡에 저장해둔 외부 블로그/가이드 원문에서 나온 설치·실행 명령어다. **모두 원문 그대로 보존.**

- (24221~24390) MengTo의 Agent Skills 오픈소스 라이브러리(`github.com/MengTo/Skills`) 소개 — web-design(62개)/codex(10개)/ui(1개)/media(2개) 카테고리, SKILL.md 복사해서 Claude/Cursor/ChatGPT에 붙여넣는 사용법. 별도 설치 명령어는 없음(복붙 방식).

- (25376~25444) **VFF(Value-for-Fable) 설치** — Claude Code 터미널에서 실행:
```bash
cd /tmp && git clone https://github.com/itsinseong/value-for-fable.git
```
```bash
# 폴더 만들기
mkdir -p ~/.claude/skills/itsvff
mkdir -p ~/.claude/agents
mkdir -p ~/.claude/output-styles
mkdir -p ~/.claude/hooks

# 파일 복사
cp /tmp/value-for-fable/skills/itsvff/SKILL.md ~/.claude/skills/itsvff/SKILL.md
cp /tmp/value-for-fable/agents/itsvff.md ~/.claude/agents/itsvff.md
cp /tmp/value-for-fable/output-styles/vff-v2.md ~/.claude/output-styles/vff-v2.md
cp /tmp/value-for-fable/hooks/reminder.sh ~/.claude/hooks/reminder.sh

# 실행 권한 부여
chmod +x ~/.claude/hooks/reminder.sh
```
`~/.claude/settings.json`에 추가할 훅 등록:
```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "/bin/bash /Users/여기에본인계정명/.claude/hooks/reminder.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
```
확인 명령:
```bash
ls ~/.claude/skills/itsvff/   # SKILL.md 있으면 OK
ls ~/.claude/agents/          # itsvff.md 있으면 OK
ls ~/.claude/output-styles/   # vff-v2.md 있으면 OK
```
사용법: `/model sonnet` → `VFF`(발동) / `VFF 해제`(끄기) / 상시적용은 `/config` → Output style → `vff-v2` 선택.
업데이트 명령:
```bash
cd /tmp/value-for-fable && git pull
cp output-styles/vff-v2.md ~/.claude/output-styles/vff-v2.md
cp skills/itsvff/SKILL.md ~/.claude/skills/itsvff/SKILL.md
```
빠른 참조 카드(전체 한번에):
```bash
git clone https://github.com/itsinseong/value-for-fable.git /tmp/value-for-fable
mkdir -p ~/.claude/{skills/itsvff,agents,output-styles,hooks}
cp /tmp/value-for-fable/skills/itsvff/SKILL.md ~/.claude/skills/itsvff/
cp /tmp/value-for-fable/agents/itsvff.md ~/.claude/agents/
cp /tmp/value-for-fable/output-styles/vff-v2.md ~/.claude/output-styles/
cp /tmp/value-for-fable/hooks/reminder.sh ~/.claude/hooks/
chmod +x ~/.claude/hooks/reminder.sh
```
(이 VFF 블록 전체가 25223~25664, 26362~27054에 두 번 중복 게재됨 — 내용 동일)

- (25683~25735) **NVIDIA 무료 API(build.nvidia.com) 연동**: 브라우저에서 build.nvidia.com 접속 → 구글 로그인 → Models → Nemotron 3 Ultra → Get API Key 로 `nvapi-`로 시작하는 키 발급. Claude Code에 붙여넣을 프롬프트(원문):
```
nvidia_chat.py 라는 파이썬 스크립트를 만들어줘.
기능: 터미널에서 계속 질문을 입력받고, requests 라이브러리로
https://integrate.api.nvidia.com/v1/chat/completions 에 POST 요청을 보내서
답을 받아 출력하는 무한 반복 채팅 프로그램이야.
model은 "nvidia/nemotron-3-ultra-550b-a55b" 로 고정해줘.
API 키는 코드에 직접 쓰지 말고 환경변수 NVIDIA_API_KEY에서 읽어오게 해줘.
requests 패키지가 없으면 설치 안내도 같이 해줘.
```
실행:
```
export NVIDIA_API_KEY="여기에_붙여넣기"
python3 nvidia_chat.py
```
(윈도우는 `set NVIDIA_API_KEY=여기에_붙여넣기` / `python nvidia_chat.py`)

- (25764~25836) **Pixel Agents + CloudCLI (세션 시각화 도구)** 설치 — Claude Code에 붙여넣는 프롬프트:
```
새 컴퓨터에 Claude Code만 있어. node --version으로 버전을 확인해줘. v20 이상이면 그냥 넘어가고, 없거나 낮으면 nvm을 설치해서 최신 LTS Node를 깔아줘.
```
```
~/apps 폴더에 https://github.com/pixel-agents-hq/pixel-agents 저장소를 클론해줘. 그 폴더에서 npm install, npm run build까지 실행하고, 끝나면 node dist/cli.js --port 3100 으로 백그라운드에서 실행해줘. 다 되면 http://localhost:3100 에서 픽셀아트 사무실 화면이 뜨는지 확인해줘.
```
```
@cloudcli-ai/cloudcli 패키지를 npm으로 전역 설치해줘. 설치되면 cloudcli 명령으로 실행해줘. 콘솔에 뜨는 Server URL을 알려줘.
```
연결: Pixel Agents(localhost:3100) → Settings → Remote Session Viewer URL에 `http://localhost:3001` 입력.

- (25861~25929) **Nemo Code (클로드코드용 무료 모델 프록시)** 설치 — NVIDIA API 키 발급 후:
```
node --version, python3 --version을 확인해줘. node는 18 이상, python은 3.8 이상이어야 해. 없거나 낮으면 각각 nvm(node)·pyenv 또는 시스템 패키지 매니저(python)로 설치해줘.
```
```bash
curl -fsSL https://raw.githubusercontent.com/kevdogg102396-afk/free-claude-code/master/install.sh | bash
```
(Windows: `irm https://raw.githubusercontent.com/kevdogg102396-afk/free-claude-code/master/install.ps1 | iex`)
모델 지정 프롬프트:
```
NEMO_MODEL 환경변수를 nvidia/nemotron-3-super-120b-a12b 로, NVIDIA_API_KEY 환경변수를 내가 알려준 값으로 설정해줘. 그다음 clawdworks run "간단한 파이썬 함수 하나 짜줘" 로 실제로 잘 도는지 테스트해줘.
```
alias 등록(`~/.zshrc` 또는 `~/.bashrc`):
```bash
alias claude-nemotron='NEMO_MODEL=nvidia/nemotron-3-ultra-550b-a55b clawdworks'
alias claude-glm='NEMO_MODEL=z-ai/glm-5.2 clawdworks'
alias claude-minimax='NEMO_MODEL=minimaxai/minimax-m2.7 clawdworks'
```
주의사항 원문: 무료 모델은 Claude가 아니며 프롬프트 인젝션에 더 취약, Anthropic 비공식·비제휴. 개인 PC에선 Docker 모드(격리) 권장.

- (26052~26212) **클로드 코드 종합 가이드(비개발자용)** — 설치는 claude.com/download에서 데스크탑 앱 다운로드 후 "Code" 탭 사용(터미널 불필요). 핵심파일 3개(CLAUDE.md/about-me.md/voice.md). CLAUDE.md 기본 템플릿:
```
# 작업 규칙

## 시작 전 항상
- about-me.md 와 voice.md 를 먼저 읽어라.
- 바로 실행하지 말고, 불명확하면 먼저 질문해라.

## 결과물
- 문서·콘텐츠는 한국어로. 웹은 HTML 한 파일로.
- 항상 내가 바로 쓸 수 있는 완성형으로.

## 하지 말 것
- 내 허락 없이 파일 삭제 금지.
- "할 수 있습니다" 같은 늘어지는 AI 말투 금지.
```
about-me 인터뷰 프롬프트(7개 질문 순차) 및 스킬 생성 예시(`~/.claude/skills/carousel/SKILL.md`) 원문 포함. CLAUDE.md 레벨업 추가규칙(파일저장규칙/재시도상한/먼저찾고물어봐라/결과물보고) 및 settings.json 훅 예시:
```json
{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ~/my-scripts/session-start.sh"
          }
        ]
      }
    ]
  }
}
```
추천 스킬 9개 언급(Superpowers, voice-builder, skill-creator, Charlie Hills Social Media OS, hook-generator, Humanizer, canvas-design, GEO/SEO Claude, PM Skills Marketplace). 설치는 `/plugin marketplace add <owner>/<repo>` 또는 `~/.claude/skills/`에 클론.

- (24455~24812, 세컨드브레인/Obsidian 볼트 구축 가이드): Karpathy식 "지식베이스를 코드베이스처럼" — vault 폴더 구조(raw/entities/concepts/INDEX.md), CLAUDE.md 4줄 글쓰기 규칙, `/goal` 백필 프롬프트, extract-approach 스킬(`.claude/skills/extract-approach/SKILL.md` + CLAUDE.md에 "after every non-trivial solved problem, run the extract-approach skill" 연결) — 이미 프로젝트에서 쓰는 extract-approach 스킬의 유래로 보임.

- (27178~27231) **Fable5용 "나에게 맞춘 개인지침" 생성 프롬프트**(claude.ai 프로필>설정>Claude 지침에 붙여넣는 용도, 원문 영문 지침 포함) 및 **"Fable의 뇌로 생각하게" 만드는 프로젝트 지침 프로토콜**(6단계 answer-before/작업중 규칙, 문서가 여기서 끊김 — 다음 구간에 이어질 가능성 있음).

## ③ 혜미 파악

- ~~맥미니를 구입해 작업환경을 구축하고, `github.com/jung-wan-kim/teamclaude`를 이용해 클로드 계정을 다중으로 추가해 순환 사용하도록 세팅함(2026-07-06)~~ → **❌ 혜미 정정(2026-07-18): 이 항목은 혜미 얘기가 아니라 스레드(Threads)에서 퍼온 글을 잘못 추출한 것.** 실제로는 혜미가 2026년 8월부터 삼성 노트북(32GB RAM / 512GB)을 사용할 예정.
- 과거에 ChatGPT 팀 사이트로 "온천꽃식물원" 주문 앱을 직접 만들어본 적이 있음(API 미사용이라 정확도가 낮았다고 스스로 평가) — 꽃집(온천꽃식물원)이 실제 운영 사업이며, 그 이전에도 자동화 시도가 있었음을 보여줌.
- 지역 사회/행정 네트워크가 있음: 경조사(승진 축하금, 문상 등) 관련 음성 지시가 다수 등장 — 창녕군청 재무과, 부곡면장, 북면장, 새마을협의회 등과 친분. 온천꽃식물원이 위치한 지역(창녕/부곡)의 공무원·지역 인맥을 통해 화환·경조사 관련 주문이 들어오는 구조로 추정됨(꽃집 사업과 지역 네트워크가 밀접히 연결).

---

# 구간 5 (27233~33268줄)

## ① 프로젝트 관련

### A. 외부 자료 다량 붙여넣기 (27233~30224줄) — 대부분 잡담 아닌 "인테이크 후보" 외부 자료
이 구간 앞부분은 혜미가 인스타/노션 등에서 긁어온 외부 블로그·PDF 콘텐츠를 통째로 붙여넣은 것들임. 운영표 §4 인테이크 파이프라인(평가→보관→채택분만 반영) 대상. 프로젝트 결정사항은 아니고 "읽은 자료" 성격.
- (27251~27338줄) Fable5용 프롬프트 세팅 가이드 — 글쓰기/디자인 스타일 역설계, 프로젝트 지침 업그레이드, 프롬프트 모음집 만들기용 메타프롬프트 3종 (내용은 범용 템플릿, 코드블록 원문 그대로 있음, 필요시 재사용 가능)
- (27374~28163줄) "Claude Cowork로 시작하기" 가이드 — Cowork 개념, 프로젝트 지침 작성법, 외부 도구 연결 3방식(네이티브 커넥터/Zapier MCP/커스텀 MCP), Gmail·Notion·Calendar·Slack·Canva 등 커넥터별 실전 패턴과 프롬프트 40여개 (일반 참고자료, 혜미의 프로젝트 결정 아님)
- (28164~28720줄) "MARGIN AI — 24시간 일하는 AI 비서 30명" 자동화 조합표 (역시 외부 자료)
- (28723~29439줄) "페이블 5 공식 복붙 프롬프트 모음" — 모델 전환 시 버릴 습관 9개, 공식 프롬프트 12종(의도제공/과잉계획방지/결론부터보고/증거기반보고/검증자서브에이전트/멈춤기준/경계명시/병렬위임/과잉정리방지/메모리시스템/조기중단방지/컨텍스트안심문구). 이 프롬프트들은 혜미의 실제 AI 운영 원칙에 실제로 반영되어 있다고 본인이 언급(29274줄 "제 작업에 실제로 박혀 있는 것들").
- (29392~29426줄) Fable 남은 사용량 활용 30개 우선순위표 — SYSTEM_PROMPT_V2.md, SKILLS_AUDIT_TABLE.md 등 산출물 목록 (범용 가이드, 혜미가 이후 실제로 이 틀을 응용해 7/11 작업 진행함)
- (29440~29612줄) Sonnet 5 + Higgsfield MCP 가이드 — 사진 한 장으로 캠페인 광고 만들기 (외부 자료, 꽃집/콘텐츠 공장에 참고될 수 있음)
- (29615~29822줄) Kimi Slides 사용법 가이드 (외부 자료)
- (29824~29937줄) 코드잇 스프린트 AI 엔지니어 과정 광고 (국비지원, 외부 광고 자료 — 혜미 관련 결정 없음)

### B. 실제 프로젝트 작업 — 2026-07-08 낮 시간대, AI공장 옵시디언 이식 작업 (31283~33268줄)
- 이 채팅(claude.ai 프로젝트)과 Cowork/Claude Code가 서로 다른 저장공간이라 파일이 자동 연결 안 됨을 재확인. "구현작업_종합자료_0708.md"를 채팅 프로젝트 지식에는 넣었으나 Cowork가 보는 실제 폴더에는 별도로 넣어야 함.
- AI공장 옵시디언 노트 18개(zip)를 생성해 혜미에게 전달함 — 플랫폼(범용러너 스펙+공통규칙 3가지) 4개, 공장 12개(정부지원/꽃집/방역+신규9개, 각 stages/절대규칙/미결사항), 미해결항목 노트, 작업일지 노트 포함.
- 혜미가 "이걸 자동화 해줘야는게 니일이잖아"라고 지적 → Cowork에 넣을 "옵시디언 자동 반입 시스템" 지시문을 작성함: 다운로드 폴더 감시 → zip 자동 압축해제 → 볼트의 AI공장/ 폴더로 이동(삭제 금지, 같은 이름은 날짜 붙여 나란히 저장) → 처리 완료본은 별도 폴더로 → 볼트에 자동화로그.md 기록. (33359~33380줄 부근, 실제로는 31359~31381줄)
- 정부지원사업 GitHub 앱 연동(jbjw) 작업: `eligibility_keyword_safety_check()` 함수를 govskill_adapter.py에 추가 완료 — 공고의 eligibility/exclusions/raw_text에서 "업력/매출/지역/체납" 4개 핵심 키워드 언급 여부만 확인, 미확인 시 경고 표시(base confidence는 안 건드림). GitHub 앱 기존 테스트 77/77 통과, gov-support-skill 5개 회귀 커맨드 전부 기존값과 동일.
- 서류 준비상태(PSST-Support) 필드 매핑은 GitHub 앱 데이터에 대응 필드가 없어 매핑하지 않기로 결정(억지 매칭은 새 판정 로직을 만드는 셈이라 판단) — translation_notes에 "데이터 없어 항상 미확인 처리됨" 명시만 함.
- **미해결 이슈**: jbjw-main 폴더가 GitHub zip 압축 해제본이라 `.git`이 없어 이번 수정분(eligibility_keyword_safety_check)이 커밋되지 않은 상태로 남음 — 데이터 유실 위험, 다음 세션 최우선 처리 항목으로 남김.
- Fable 5 무료 사용 마감이 원래 7/7에서 7/12로 5일 연장됐다는 소식을 웹검색으로 확인·전달함(Forbes, The New Stack, Anthropic 공식 X 계정 근거). 단 Anthropic 공식 지원 페이지는 아직 구날짜(7/7) 표시 상태.
- 외부에서 붙여넣은 "비개발자용 AI 활용 가이드" 문서에 대해, 혜미가 이미 그 가이드의 1~4순위(전체지도/운영매뉴얼/컨설팅로드맵/기획서)를 다 앞서 진행했다고 판단해 "처음부터 다시 하지 말 것"을 권고함. 다만 5순위(콘텐츠 시스템)는 이미 설계된 "콘텐츠재생산 공장"으로 대체 가능하다고 재평가.
- 콘텐츠재생산 공장의 외부도구(shorts_edit/cards_render)를 유료 API 대신 오픈소스 도구로 대체할지 검토하는 Fable 질문 프롬프트를 작성함(33238~33257줄) — ffmpeg(영상, 이미 오픈소스) vs 카드뉴스 렌더링 오픈소스 대안, kind:tool(external) 정의를 "로컬 실행 도구 호출"로 바꿔야 하는지, 지인 기기별 설치부담 최소화 방법을 질문.
- 옵시디언 이식 계획: 오늘 나온 설계 결정을 노트 단위(아이디어 1개=노트 1개, 5줄 이하, 관련 키워드로 연결)로 쪼개 `AI공장/` 폴더 아래 `플랫폼-범용러너-스펙.md`, `플랫폼-공통규칙-재실행게이트.md` 등으로 저장하기로 시작(구간 끝, 33260~33268줄에서 목록 작성 중 잘림 — 다음 구간에 이어질 가능성 높음).

### C. 실제 프로젝트 작업 — 2026-07-11 토요일, "범용 러너"·신규 9개 공장 설계 마라톤 (30225~32976줄) — 이 구간 최대 분량 핵심 내용
Fable 5 무료 quota 소진 마감(당시 착각: 오후4시 vs 실제로는 금 오전5:59 vs 나중에 7/12로 재연장 확인 — 결국 오후 4시로 확정하고 진행)에 맞춰, 오푸스 4.8이 짠 시간표(수면 6~7시간 포함, 페이블 새 창 20~25개 병렬 호출)를 따라 하루 종일 설계 확정 작업을 진행함.

**핵심 결정사항(전부 Fable 5에게 확정받음, "왜 Fable이 필요했는지" 근거 포함):**
1. **범용 러너 설계 감사** — 기존 설계(stages/kind/의존관계)는 "무엇을 실행하는가"는 확정됐지만 "단계 사이 데이터 모양을 기계가 검증하는 계층"이 없다고 판정. 보완 4가지 확정: ①단계별 입출력 스키마를 JSON Schema로 manifest에 선언, 러너가 실행 전 정적 검사 ②어댑터를 `kind: adapter`/`transform` 필드로 1급 시민화 ③외부 경계에 상대방 스키마 버전/체크섬 고정 ④kind 목록에 `tool(external)` 누락 여부 확인 필요. MVP 범위도 확정(정적검증+pipeline-type만 지원+local/model 실행+human은 스텁+스키마검증 실패시 정지).
2. **공통규칙 3가지 확정**: ① 재실행 게이트(rerun_gate — 해시 비교로 skip/run) ② 반려 루프 라우팅(on_reject — 사유코드별 되돌아갈 stage 지정, max_loops+on_exhaust) ③ 검수원 등급(review.risk_tier — T1/T2/T3, 위험도=피해크기×회수가능성 축, 실제 값 배정은 보류).
3. **신규 9개 공장 stages 설계를 전부 Fable로 확정**: 아이디어(6단계, 전용 평가원+기획원 필요), 자료정리+학습변환(원래 별도였으나 최종적으로 "템플릿 공유, 런타임(골격) 분리" 결론 — 3축 파라미터 ordering/arrival/has_speaker로 통합), PPT(8단계, 포장원=local 확정), 가구배치(6단계, 판정 코어는 model 절대 금지), 챗봇·AI직원(구축+운영 이중구조), 이미지·AI인간(7단계, 저작권검수 이중배치), 3D캐릭터·굿즈(9단계, 저작권게이트 2개), 콘텐츠재생산(팬아웃 3갈래: 숏폼/블로그/카드뉴스, 셋 다 "분석카드" 하나에서만 분기), 냉장고·집밥(7단계, 파이프라인형+공유장부 낙관적 동시성).
4. **"앱형·소수공유" 공통 조건이 이날 처음 확정됨** — 전체 14개 공장 모두 "제작자 본인 주사용자 + 지인 소수 공유 가능, 앱스토어 출시는 아님"이라는 전제. 결론: stages 분류 체계는 안 바꿔도 됨, 추가되는 건 데이터 네임스페이스(user_id 스코프)/human 검수는 데이터소유자 담당/외부도구 자격증명 분리/모델비용 사용자별 상한/장부 personal·shared 구분 5가지뿐. **"각자 설치형이면 인증 자체가 불필요"**가 핵심 결론 — OAuth·로그인 시스템은 지금 단계에서 만들 필요 없음(나중에 "여러 명이 한 서버 공유 접속"으로 바뀔 때만 PIN 수준 인증 추가).
5. **자료정리 공장·학습변환 공장 최종 재설계**: 자료정리는 원래 "업로드→질문하면 답변"(상주형) 설계였으나, 실제 목적이 "쌓아둔 자료(인스타 캐러셀/릴스/노션 등 형식 제각각) 전체를 훑어 초보자 기준 학습 로드맵을 종합 생성"하는 것으로 밝혀져 파이프라인형으로 전면 재설계됨. 학습변환은 "강의 회차별 정리본 + 누적 종합본(강사 화법 유지)" 목적으로, 종합본을 매번 재요약하면 복리손실 발생하므로 "course-state 갱신 + 영향섹션만 patch, 5회차마다 전체 재빌드" 구조로 확정.
6. **방역 스킬**: 선택형 봇 3개(방문지다중분리/현장사진첨부/견적산정)는 골든셋(실사례) 미확보로 활성화 보류 결정 — 근거: 혜미가 확인해준 "합성 테스트 12건은 가정해서 만든 것이지 실제 업무 기록이 아님"이라는 사실에 따라, 근거 없이 판단하지 않기로 함(HANDOFF.md에 "골든셋 확보 전까지 보류" 명시 예정).
7. **"구현작업_종합자료_0708.md"** 파일로 이날 전체 결정을 종합해 Cowork 작업 지시서로 만듦(전체그림/플랫폼공통스펙/①②③ 상태/신규11개 공장 stages 전체/소수공유조건/미해결항목/Cowork 작업순서 8개 섹션).
8. **Cowork 작업 권장 순서 확정**: 범용러너 MVP 구현 → 공통규칙 3가지 반영 → 정부지원사업(한계 1·2번 수정+공통규칙 소급+회귀테스트) → 방역(공통규칙만, 3개봇 안 건드림) → 꽃집(manifest 신규 작성+14 stages 이관) → 신규 11개(순서 무관, 필요한 것부터).
9. **오늘 일부러 안 한 것(미결 항목 5개, 명시적으로 보류)**: 방역 3개봇 활성화(골든셋 없음), human 검수 큐/SLA 집행 시스템(스키마 골격만), risk_tier 값의 14개 공장 실제 배정(①③만 확정), 정부지원사업 내용품질 반려사유코드(judge 확장 선행 필요), 반려루프 사유코드 enum 전체 통일.

## ② 클로드 스킬/설정

**Claude Code 설치 (30146~30157줄, 원문 그대로):**
```bash
npm install -g @anthropic-ai/claude-code
```
```bash
cd your-project
claude
```
(로그인은 Claude Pro 계정으로. 설치 후 Claude Code가 코드 읽고 명령어 실행하고 파일 수정 가능해짐 — 이 가이드에서는 Opus 4.7 기준 설명)

**OpenAI Codex 관련 (외부 툴, 참고용, 30134~30224줄):**
- Codex 앱: OpenAI Codex 공식 페이지에서 다운로드(맥용, ChatGPT Plus 등 아무 구독이나 있으면 무료)
- "Codex 앱 안에서 터미널 열고 그 안에서 Claude Code 실행" 조합 권장
- .codex/config.toml에서 `reasoning_effort = "medium"` 설정 팁 (가벼운 작업용)
- 이 가이드의 결론: Claude=설계/기획, Codex=구현/디버깅/리팩터링 — Claude Pro($20)+ChatGPT Plus($20) 조합이 단일 $200 요금제보다 오래 버틴다는 주장. CLAUDE.md에 프로젝트 규칙·스택·제약사항 저장 권장(이미 이 볼트에서 실행 중인 관행과 일치).

**Higgsfield MCP 연결 (29494~29532줄, 원문 그대로):**
- 방법 A(터미널 없이, Claude 웹·Cowork): 주소 `https://mcp.higgsfield.ai/mcp` 를 Claude 설정 → 사용자 지정 → 커스텀 커넥터 추가에 붙여넣기, 이름 `Higgsfield`로 설정 후 로그인
- 방법 B(터미널, Claude Code):
```
npm install -g @higgsfield/cli
```
```
higgsfield auth login
```
(Higgsfield 구독/크레딧 별도 필요. 사진 한 장 기준 제품컷→인물+제품컷→광고영상(Seedance 2.0) 3단계로 캠페인 세트 생성 가능. 안전필터 이슈 시 "목선 높은 상의/노출 없음" 문구 추가 권장.)

**기타 커넥터 설치 방식 (외부 가이드 요약, 원문 명령어 형태는 아님, 27418~28163줄):**
- 네이티브 커넥터: 설정 > Connectors > Browse connectors에서 앱 클릭 → OAuth 로그인 (Gmail은 발송 불가, 초안까지만/ Google Calendar는 생성·수정 가능/ Google Drive는 읽기만 편집 불가/ Notion은 생성·검색/ Slack은 Pro 이상)
- Zapier MCP: zapier.com/mcp에서 New MCP Server > Claude 선택 → 생성된 주소를 설정 > Add custom connector에 붙여넣기 (무료 플랜은 커스텀 커넥터 1개까지)
- PlayMCP 카카오: Claude 설정 → 커스텀 커넥터에 `playmcp.kakao.com` 도구함 등록 → 카카오 로그인 (본인 OAuth 기반이라 완전 무인 자동화·크론 예약 발송 불가, 아직 베타 — 중요 업무엔 비권장)
- DocuSign: 커스텀 커넥터에 `mcp-d.docusign.com/mcp` 등록 + DocuSign 인증 (Claude Pro 이상 필요, Open Beta, "초안"과 "발송" 상태 혼동 주의)
- Firecrawl: 커스텀 커넥터 등록 + API 키 필요

## ③ 혜미 파악

- **방역&클린 사업은 혜미 본인 사업이 아님(신규 확인)**: 32590줄에서 "가정해서 한거야 내가 하는 사업이아니라 확실하게 말못해"라고 명시적으로 밝힘 — 즉 방역 골든셋(실사례 빈도) 데이터를 혜미가 직접 제공할 수 없는 이유가 "그건 자신의 사업이 아니기 때문"임. 방역 스킬은 다른 사업자(고객/의뢰인)를 위해 만들고 있는 프로젝트로 보임. (32531줄 부근 "정기고객 약 110곳, 월 4~5건 신규 견적 문의" 등의 수치는 실제 확인된 통계가 아니라 설계 논의 중 언급된 예시/가정 수치일 가능성이 높음 — 확정 사실로 취급 금지)
- **모든 신규 14개 AI공장은 "혼자 쓰다가 유용하면 소수 지인(가족·친구·동료)에게 나눠줄 수 있어야 함, 단 앱스토어 출시용은 아님"이라는 조건이 이날 처음 명시적으로 확정됨** (31135, 31257, 31779줄 등) — 이는 향후 모든 공장 설계·구현에 적용되는 새 제약 조건으로, 로그인/인증 시스템은 "각자 설치형"을 기본 전제로 하고 당장 만들 필요 없음.
- **자료정리 공장의 실제 개인 용도**: 인스타 캐러셀, 릴스, 노션 메모 등 형식이 제각각인 자료를 평소에 많이 저장해두는 습관이 있고, 한 주제(예: "클로드 사용법")로 모아 "지금 입문 단계 기준으로 뭐부터 시작할지" 종합 로드맵을 받고 싶어함 — 저장은 하지만 활용은 못 하고 있는 정보 축적 패턴을 시사.
- **학습변환 공장의 실제 개인 용도**: 여러 회차로 진행되는 강의(온라인 강의 등)를 들으며, 회차별 정리본과 "지금까지 누적된 종합본"(강사의 화법·스타일 유지)을 동시에 원함 — 실제로 다회차 강의를 수강 중이거나 자주 수강하는 것으로 보임.
- **업무 스타일/컨디션 관련**: 7/11 자정 무렵 Fable quota 소진 마감을 앞두고 밤새 작업하려는 경향을 보였고(91% 잔여quota를 오후4시 전까지 다 쓰겠다는 압박), AI가 "수면 6~7시간을 꼭 넣어야 한다"고 여러 차례 강하게 권고함. 마감 시각(오후4시 vs 금요일 새벽5:59)을 혼동해 감정적으로 반응한 대목("정오라고.....!!!!!!!!!" 등)이 있음 — 시간 압박 상황에서 조급해지는 경향.
- **두 개의 Claude 계정을 용도별로 병행 사용**: 한 계정은 Cowork(코드 작업, 세션 리밋 자주 걸림)용, 다른 계정은 "질문 채팅용"(설계 논의)으로 구분해 운용하며, Fable quota가 계정별로 다르게 남아있어 헷갈렸던 정황 있음.
- **Fable 응답창은 매 질문마다 새 창을 열어야 한다는 원칙을 실제로 체화해 하루 종일 지킴** — "한 창에 몰아넣지 말고 질문 하나당 새 창" 규칙을 여러 차례 스스로 재확인.
- **작업 결과를 옵시디언 볼트에 자동 반입하는 시스템을 원하며, "이것도 자동화 해줘야 하는 게 니 일이잖아"라고 직접 지적** — 수동 개입(다운로드 클릭 등) 최소화를 강하게 원함, 반자동이라도 자기 몫이 "드래그 한 번"으로 줄어드는 것에 만족.
- **오픈소스 도구 활용에 관심**: "오픈소스를 다운받아서 쓰는 방법 알려줘"(클로드버전/코덱스버전/수동버전 비교 요청, Threads 링크 첨부, 30042~30047줄), 콘텐츠재생산 공장의 이미지·영상 처리도 유료 API 대신 오픈소스로 대체하고 싶어함 — 비용에 민감하고 직접 실험해보려는 성향.

---

# 구간 6 (33269~39301줄)

## ① 프로젝트 관련

- (33269~33334) "구현작업_종합자료_0708.md"를 옵시디언 노트 16~18개로 쪼개는 작업 논의 및 실행 완료. 최종 산출물: 플랫폼(범용러너 스펙+공통규칙3가지) 노트 4개 + 공장 12개 노트(정부지원/꽃집/방역+신규9개) + 미해결항목 노트 + 작업일지 노트, zip으로 다운로드 제공. Fable이 아닌 일반 작업(기계적 분할)으로 처리 — "판단 필요한 곳에만 Fable" 원칙 재확인.
- (33685~34167) "구현작업용 종합자료 — 2026-07-08" 전문 — AI 공장 플랫폼 설계 확정본:
  - 파일구조: SKILL.md/manifest.yaml/scripts/templates/checks
  - manifest.yaml 필수필드: io_contract, model_routing(티어명만, Fable 배정 절대금지), quality_gate, invocation, triggers, pipeline.stages
  - stage kind 4종: local/model/human/tool(external), 골격 2종: pipeline-type/resident
  - 오늘 확정한 공통규칙 3가지: ①재실행게이트(rerun_gate) ②반려루프라우팅(on_reject, 사유코드+max_loops+on_exhaust) ③검수원등급(review.risk_tier: T1/T2/T3)
  - 범용 러너 MVP 범위 확정, 어댑터(외부앱 연결) 패턴 확정
  - 14개 공장별 stage 설계 확정본 전문: ①정부지원사업(90%완료, 한계4가지: 자격조건자유서술/서류준비상태필드없음/founded_date근사/budget_plan category없음) ②꽃집(12→14봇 확정, 주문판정봇·주문분리봇 추가발견) ③방역(선택형봇3개 골든셋대기 보류 유지) ④신규11개(아이디어/자료정리+학습변환통합/PPT/가구배치/챗봇·AI직원/이미지·AI인간/3D캐릭터굿즈/콘텐츠재생산/냉장고집밥) 각각 stage 순서·kind·위험요소 상세
  - 소수공유(비앱스토어) 조건: 데이터 네임스페이스, human담당자=데이터소유자, 자격증명분리, 사용자별 비용귀속. 공동장부 후보: 냉장고(가족)/꽃집재고/방역작업기록
  - 오늘 남긴 미해결항목 5가지 (방역봇3개 보류, human검수큐 미구현, risk_tier값 미배정, 정부지원 내용품질 반려사유 미정, 반려루프 enum 미완결)
  - Cowork 권장 작업순서 8단계 명시
- (34119~34165) "옵시디언 자동 반입 시스템" 설계 — 완전자동은 구조상 불가(채팅이 PC 파일쓰기 권한 없음), 반자동 구조 제안: [채팅에서 zip다운로드]→[대표님이 다운로드폴더에 저장]→[감시스크립트가 압축해제→볼트 올바른폴더 이동→기존노트는 날짜붙여 병합(덮어쓰기 금지)→처리완료zip은 백업폴더 이동→자동화로그.md에 기록]. Cowork에 넘길 지시문 전문 작성됨(삭제 절대금지, 볼트경로 먼저 확인, 계획만 먼저 보여주고 승인 후 진행).
- (34503~34511) 이미 운영 중인 자동화: 매주 일요일 유튜브 링크→NotebookLM MCP로 언어별 영상 5개씩 자동생성→언어담당자별 이메일 자동발송, 구글 계정 2개 로테이션(무료모델 계정당 하루3개 제한 우회). naver-blog-pipeline: 템플릿4종→초안(md)→HTML변환→크롬예약발행→로그, 반자동화 ~99% 완성, 1건 발행완료(북블룸 소개).
- (34513~34700) ChatGPT를 Obsidian(볼트명 Hulkeinstein)에 연결하는 계획 — Claude Cowork에 넣을 지시문 전문(1단계 볼트확인→2단계 AI Context Index.md/AI Working Rules.md 생성→3단계 프로젝트표준문서(README/Context/Current Status/Decision Log/Next Actions)→4단계 비공개 GitHub저장소 "hulkeinstein-context" 동기화 준비, 민감정보 제외목록(.obsidian/, 첨부파일, API키, 개인정보, 계약서, 일기, 건강기록 등)→5단계 승인후 실행(git init, private repo 생성, Obsidian Git 플러그인)→6단계 최종검증). ChatGPT는 GitHub 연결로 문서 검색하는 방식, Cowork는 최초구축·대규모정리에만 사용 권장.
- (34484~34502) 정부지원사업 실제 심사결과 수신 — 2026년 예비창업패키지 특화분야(여성), 과제번호 20404302, 신청자 박혜미, 아이템 "회복 기록 데이터 기반 출장형 산모 회복 컨디셔닝 서비스"(모온 사업 아이템). 결과: 다수 평가위원이 차별성·경쟁우위·수익화전략 부족 지적, 서류평가 통과 어려움. 경상국립대 멘토(신은희) 심사의견도 유사(차별성/실현가능성은 인정하나 시장경쟁력강화 마케팅전략·수익성증대전략 보완 필요).
- (37497~37610) 방역 공장 실사업 데이터 — 카카오 방역 서비스 상담화면(견적문의→상담시작→약속확정 플로우 스크린샷), 방역&클린 사업자등록증(등록번호 404-46-00762, 대표 김남오, 개업일 2021-08-09, 소재지 경남 진주시 향교로 114-2), 소독증명서 샘플(대상: 비엘르모텔, 실시일 2026-06-28, 대표 박순임).
- (37884~38052) 사업자 정보 2건:
  - 대륙창업: 대표 박수길, 등록번호 608-23-72966, 개업 2013-12-20, 소재지 경남 창녕군 부곡면 온천1길 40, 사업영역: 위생(시설)관리용역/소독업/저수조청소 + 음향기계 도소매/설치/임대 + 행사대행업(이벤트·공연기획) — **혜미 정정(2026-07-18): 아빠 사업체임** (구간 2의 "방역앤클린=아빠 사업" 추정과 방역앤클린 등록증 대표 김남오와의 관계는 확인 필요)
  - 온천꽃식물원: 대표 이문숙, 등록번호 608-92-06180, 개업 2015-02-01, 소재지 경남 창녕군 부곡면 거문리, 부가세면세사업자, 사업영역: 생화·조경수 도소매(꽃다발/꽃바구니/화환/조경수/전화·방문주문 배송)
- (38053) 톡게시판 공지 계정: `k9cjhmw7z9@privaterelay.appleid.com` — 볼트 git 커밋 로그에 붙는 계정태그 `[k9cjhmw7z9]`와 동일 인물로 추정됨(다중계정 동시작업 규칙과 연결되는 단서).
- (39280~39301) 정부지원사업(bizinfo) 실데이터 연동용 API 인증키 발급 완료 — "중소기업 성공길라잡이 기업마당(Biz info)"에서 API 인증키 `X9HC6x` 발급. 지원사업 조회 API: `https://www.bizinfo.go.kr/uss/rss/bizinfoApi.do?crtfcKey=X9HC6x`, 행사정보 API: `https://www.bizinfo.go.kr/uss/rss/bizinfoEventApi.do?crtfcKey=X9HC6x` (데이터타입 미입력시 XML, 조회건수 미입력시 500개/최대500개). 이미 이전 세션 git 커밋("정부지원 bizinfo 실수집")과 연결되는 것으로 보임.

## ② 클로드 스킬/설정

**1. VFF (Value-for-Fable)** — "Fable이 단종됐다"는 잘못된 전제 위에 만들어진 문서로 확인되어 **설치 보류(비권장) 판정**. 낯선 GitHub 저장소의 셸스크립트를 UserPromptSubmit 훅에 자동실행 등록시키는 구조라 보안위험으로 지적됨.
```bash
git clone https://github.com/itsinseong/value-for-fable.git /tmp/value-for-fable
mkdir -p ~/.claude/{skills/itsvff,agents,output-styles,hooks}
cp /tmp/value-for-fable/skills/itsvff/SKILL.md ~/.claude/skills/itsvff/
cp /tmp/value-for-fable/agents/itsvff.md ~/.claude/agents/
cp /tmp/value-for-fable/output-styles/vff-v2.md ~/.claude/output-styles/
cp /tmp/value-for-fable/hooks/reminder.sh ~/.claude/hooks/
chmod +x ~/.claude/hooks/reminder.sh
```

**2. Claude Code 기본 설치**
```bash
npm install -g @anthropic-ai/claude-code
```
```bash
cd your-project
claude
```

**3. Higgsfield App Builder + Fable 5 MCP 연결**
- 커스텀 커넥터 URL(웹/Cowork): `https://mcp.higgsfield.ai/mcp`
- 터미널(Claude Code)용:
```bash
npm install -g @higgsfield/cli
higgsfield auth login
```

**4. marketingskills (마케팅 스킬 34~47개, coreyhaines31/marketingskills, MIT)**
```bash
# 전체 설치
npx skillkitinstall coreyhaines31/marketingskills
# 필요한 것만
npx skillkitinstall coreyhaines31/marketingskills --skill cro copywriting seo-audit
# 목록 보기
npx skillkitinstall coreyhaines31/marketingskills --list
```
```bash
/plugin marketplace add coreyhaines31/marketingskills/plugin
install marketing-skills
```
```bash
git clone https://github.com/coreyhaines31/marketingskills.git
cp -r marketingskills/skills/* .agents/skills/
```

**5. NotebookLM + Claude Code 연동 ("고수 셋업")**
```bash
node --version
npm --version
```
```bash
npm install -g @anthropic-ai/claude-code
claude --version
claude
```
```bash
pip install "notebooklm-py[browser]"
playwright install chromium
notebooklm login
notebooklm auth check --test --json
notebooklm skill install
# 또는
npx skills add teng-lin/notebooklm-py
```
파이프라인 예시 명령: `notebooklm create "AI 연구 노트"`, `notebooklm use <notebook_id>`, `notebooklm source add "https://www.youtube.com/watch?v=xxxxx"`, `notebooklm ask "핵심 인사이트 3가지를 요약해줘"`, `notebooklm generate audio "쉽게 설명해줘" --wait`, `notebooklm download audio ./Desktop/podcast.mp3` 등.

**6. Claude Code 상태표시줄/토큰 절약 팁**
```
/statusline show folder, git branch, model, and context & with a progress bar
```
- `/model OpusPlan` (계획은 Opus, 실제 코딩은 Sonnet 분업)
- `/compact` (긴 대화 요약해 토큰 절약)
- 프롬프트 끝에 "ultrathink" 추가 (깊은 사전 계획 강제)

**7. 세컨드 브레인(Obsidian+Claude Code, "LLM Wiki" 패턴, 원출처 Andrej Karpathy)** — 별도 설치 명령 없음. 볼트 루트 `CLAUDE.md`에 넣는 전용 프롬프트 템플릿(사서 역할, raw-sources/wiki 분리, INGEST/QUERY/LINT 3동작) 전문이 대화에 포함됨. `.md` 파일 하나에 넣고 붙여넣기만 하면 되는 방식.

**8. Claude 범용 고성능 시스템 프롬프트** — 설치 명령은 아니고, "Claude 프로젝트 지침" 또는 "스킬 지침"에 붙여넣는 대형 프롬프트 템플릿(정체성 범용화, 지시 우선순위, 검색판단, 파일처리, 안전정책 등 18개 섹션 + 플레이스홀더 정의). 전문이 대화 원문에 포함됨.

**9. 클로드 디자인 감각 스킬 42개 세팅 (@3dragon_pd 정리)** — 핵심 4개 추천 조합:
```bash
# 1) 취향 베이스 (셋 중 하나만)
npx skills add anthropics/skills --skill frontend-design

# 2) 모션
npx skills add delphi-ai/animate-skill --skill animate

# 3) 색·간격 토큰
/plugin marketplace add ComposioHQ/awesome-codex-skills   # theme-factory

# 4) 검수(눈) — 가장 중요
claude mcp add playwright -s user -- npx @playwright/mcp@latest
```
그 외 개별 스킬(레이어01~06, 총 41개 문서화) 설치 명령 목록:
```bash
npx impeccable install   # 이후 /impeccable init
npx skills add Leonxlnx/taste-skill --skill design-taste-frontend
npx skills add kylezantos/design-motion-principles
claude mcp add figma -- npx figma-developer-mcp --stdio
npx skills add Leonxlnx/taste-skill --skill brandkit
/plugin marketplace add Owl-Listener/designer-skills
/plugin install nano-banana@devon-claude-skills
/plugin marketplace add AgriciDaniel/banana-claude
npx skills add anthropics/skills --skill canvas-design
npx skills add anthropics/skills --skill algorithmic-art
/plugin marketplace add DojoCodingLabs/remotion-superpowers
npx create-video@latest   # Blank 템플릿, Skills 설치
npx skills add LobzyJay/motion-design-with-claude --skill blender-motion
npx skills add LobzyJay/motion-design-with-claude --skill aftereffects-motion
npx skills add Owl-Listener/ai-design-skills --skill context-window-design
npx skills add Owl-Listener/ai-design-skills --skill conversation-patterns
npx skills add Owl-Listener/ai-design-skills --skill generative-ui
npx skills add Owl-Listener/ai-design-skills --skill progressive-disclosure
npx skills add Owl-Listener/ai-design-skills --skill multimodal-orchestration
npx skills add Owl-Listener/ai-design-skills --skill mixed-initiative-flow
npx skills add Owl-Listener/ai-design-skills --skill frustration-detection
npx skills add Owl-Listener/ai-design-skills --skill feedback-loops
npx skills add Owl-Listener/ai-design-skills --skill system-prompt-structure
npx skills add Owl-Listener/ai-design-skills --skill persona-architecture
npx skills add Owl-Listener/ai-design-skills --skill tone-calibration
npx skills add Owl-Listener/ai-design-skills --skill emotional-design
npx skills add Owl-Listener/ai-design-skills --skill template-design
npx skills add Owl-Listener/ai-design-skills --skill few-shot-patterns
npx skills add Owl-Listener/ai-design-skills --skill chain-of-thought-design
npx skills add Owl-Listener/ai-design-skills --skill constraint-specification
npx skills add Owl-Listener/ai-design-skills --skill guardrail-design
npx skills add Owl-Listener/ai-design-skills --skill trust-calibration
npx skills add Owl-Listener/ai-design-skills --skill transparency-patterns
```
(42개 다 깔지 말고 3~4개만 권장. 레이어03 "클로드 디자인/캔버스" 4종은 claude.ai/design 웹 전용, CLI 설치 없음.)

**10. Anthropic 공식 GitHub 학습 경로 안내**
```bash
mkdir anthropic-learning
cd anthropic-learning
git clone --depth 1 https://github.com/anthropics/courses.git
git clone --depth 1 https://github.com/anthropics/claude-code.git
```
```bash
# macOS/Linux 권장 설치
curl -fsSL https://claude.ai/install.sh | bash
# Homebrew
brew install --cask claude-code
```
```bash
cd anthropic-learning
claude --permission-mode plan
```
```
/plugin marketplace add anthropics/skills
/plugin install example-skills@anthropic-agent-skills
```
```
/plugin install document-skills@anthropic-agent-skills
```
(둘 다 한꺼번에 설치할 이유는 없음 — 문서작업이 목적이면 document-skills만)

**11. Everything Claude Code (ECC, affaan-m/ECC, MIT)** — 검증버전 기준(2026-07-15 재테스트 완료).
```
/plugin marketplace add https://github.com/affaan-m/ECC
/plugin install ecc@ecc
/reload-plugins
/plugin list
```
rules 별도 복사(플러그인이 자동배포 못함), macOS/Linux/WSL:
```bash
git clone https://github.com/affaan-m/ECC.git
cd ECC
mkdir -p ~/.claude/rules/ecc
cp -R rules/common ~/.claude/rules/ecc/
cp -R rules/typescript ~/.claude/rules/ecc/
```
Windows PowerShell:
```powershell
git clone https://github.com/affaan-m/ECC.git
cd ECC
New-Item -ItemType Directory -Force -Path "$HOME/.claude/rules/ecc" | Out-Null
Copy-Item -Recurse rules/common "$HOME/.claude/rules/ecc/"
Copy-Item -Recurse rules/typescript "$HOME/.claude/rules/ecc/"
```
가벼운 수동 선택 설치(macOS/Linux/WSL):
```bash
git clone https://github.com/affaan-m/ECC.git
cd ECC
./install.sh --profile minimal --target claude --dry-run
./install.sh --profile minimal --target claude
```
Windows PowerShell:
```powershell
git clone https://github.com/affaan-m/ECC.git
cd ECC
.\install.ps1 --profile minimal --target claude --dry-run
.\install.ps1 --profile minimal --target claude
```
스킬 지정 설치:
```bash
./install.sh \
  --target claude \
  --skills tdd-workflow,security-scan,continuous-learning-v2 \
  --dry-run
```
다른 도구(Cursor/Codex/Gemini) dry-run:
```bash
./install.sh --profile minimal --target cursor --dry-run
./install.sh --profile minimal --target codex --dry-run
./install.sh --profile minimal --target gemini --dry-run
```
OpenCode: `npm install -g @higgsfield/cli` (오탈자 아님, 문서 원문은 `npm install ecc-universal`)
```bash
npm install ecc-universal
```
검증/삭제:
```bash
claude plugin list
claude plugin details ecc@ecc
```
```
/plugin uninstall ecc@ecc
```
```bash
node scripts/uninstall.js --dry-run
node scripts/uninstall.js
```
업데이트 관련:
```bash
claude --version
claude update
npm install -g @anthropic-ai/claude-code@latest
```

**12. Fable 5 Agent 설계 공략집 (@lilcherry.ai.maker)** — 설치 명령 없음. Lv.1~Lv.5 단계별 복붙 프롬프트 템플릿 모음(서류읽기/초안피드백/행사기획/업무시스템화/SKILL만들기/AGENT설계/승인게이트/실패감지 등). 참고: Fable5 무료 포함 프로모션 종료일이 7/19 오후 3:59(PT)로 재차 연장됐다는 타임라인 정보 포함(6/9 공개→6/12 수출통제 중단→7/1 재배포→7/7,7/19 연장).

**13. Graphify (코드베이스 지식그래프 도구, tree-sitter AST 기반)**
```bash
python3 --version   # 3.10 이상 필요
uv --version
claude --version
```
설치(macOS):
```bash
brew install python@3.12 uv
```
설치(Windows):
```
winget install Python.Python.3.12
winget install astral-sh.uv
```
설치(Ubuntu/Debian):
```bash
sudo apt update
sudo apt install -y python3 python3-venv
curl -LsSf https://astral.sh/uv/install.sh | sh
```
Graphify 본설치:
```bash
uv tool install graphifyy
graphify --version
graphify install
```
PATH 문제 시:
```bash
uv tool update-shell
```
프로젝트 단위 설치:
```bash
graphify install --project
```
`.graphifyignore` 예시:
```
node_modules/
dist/
build/
coverage/
.next/
.venv/
vendor/
*.min.js
```
그래프 생성(Claude Code 안에서 슬래시 명령):
```
/graphify .
/graphify . --mode deep
```
확인:
```bash
test -s graphify-out/graph.json && echo "graph.json OK"
test -s graphify-out/GRAPH_REPORT.md && echo "report OK"
test -s graphify-out/graph.html && echo "graph.html OK"
open graphify-out/graph.html      # macOS
xdg-open graphify-out/graph.html  # Linux
start graphify-out/graph.html     # Windows PowerShell
```
```
/graphify explain "실제 클래스나 함수 이름"
```
Claude Code가 그래프부터 보게 하는 훅 설치:
```bash
graphify claude install
```
`.claudeignore` 추가:
```
graph.json
graphify-out/
```
쿼리/경로/설명 명령:
```
/graphify query "인증 요청이 데이터베이스까지 가는 흐름"
/graphify query "결제 실패가 재시도되는 흐름" --dfs --budget 1500
/graphify path "AuthService" "UserRepository"
/graphify explain "AuthService"
```
터미널 직접 조회:
```bash
graphify query "show the auth flow"
graphify path "AuthService" "UserRepository"
graphify explain "AuthService"
```
갱신/훅:
```
/graphify . --update
```
```bash
graphify hook install
graphify hook status
```
```
/graphify . --watch
```
```bash
uv tool install "graphifyy[watch]"
uv tool upgrade graphifyy
graphify install
graphify hook install
```
문제해결:
```bash
uv tool uninstall graphifyy
uv tool install graphifyy
graphify install
```
```bash
graphify extract . --force
graphify cluster-only .
```
제거:
```bash
graphify claude uninstall
graphify uninstall
graphify uninstall --purge
```
(참고: PyPI 패키지명은 `graphifyy`(y 두 개)이나 CLI 명령은 `graphify`. 토큰절감 수치는 프로젝트 크기에 따라 크게 다름 — "무조건 70% 절감"은 공식 근거 없음, 공개 재현예시는 52파일 71.5배 / 4파일 5.4배 / 6파일 약1배.)

## ③ 혜미 파악

- 자신을 "초민감자"로 표현하며 심리·감정에 대한 이해도가 높은 AI를 원한다는 선호를 밝힘. 스스로를 "비개발자 무지랭이"라 칭하며, 왜 특정 모델을 썼는지·왜 그만큼의 노력(effort)이 들어갔는지를 "작업 설명"과 "채팅 설명"으로 분리해서 알려주길 원한다는 요청이 있었음(현재도 유효한 소통 방식 요구).
- 완전 초보자용으로 "스텝 0부터 10까지" 하나하나 알려주는 안내, 링크를 스피커로 읽어주는 앱, 요약 파일을 자동 제작해주는 앱 등이 있으면 좋겠다는 아이디어/니즈를 언급함.
- 자동화·바이브코딩 관련 정부지원 교육과정을 다수 검토 중(국민내일배움카드 사용, 총 4개 강좌 수강 가능, 예산 한도 50만원). "자체 서비스 제작 기준 최종 4개"로 선정한 과정: ①[AICE] 인공지능활용능력향상 패키지교육 ②[AI 능력자] 바이브코딩 with Cursor(DB·배포까지, 고객용 서비스 제작) ③[AI 능력자] 비개발자도 할 수 있는 바이브코딩 업무자동화(OCR·크롤링·엑셀 등 공장 내부기능 확장) ④n8n 바이블(기존 Python/YAML 스킬·웹서비스·외부도구 연결 통합생산라인). "Cursor+AICE"는 이미 선정했지만 변경 가능하다고 언급. AICE 자격증은 공공기관 지원사업 대비용으로 미리 취득 고려 중이나 우선순위 낮으면 포기 가능하다고 밝힘. 너무 비싸면 epass AICE(77,220원, 본인부담 19,310원, 26시간/8주 과정)로 대체 검토.
- 실제 사업 아이템 신원 확인: "모온(mo,on)"의 사업 아이템은 "회복 기록 데이터 기반 출장형 산모 회복 컨디셔닝 서비스"(산후조리 관련, 데이터 기반 맞춤형 회복 프로그램)이며, 2026년 예비창업패키지 특화분야(여성) 서류평가에서 시장 차별성·수익화 전략 부족으로 탈락한 이력이 있음. 경상국립대학교 창업 지원사업에서도 유사한 심사의견(차별성은 인정되나 마케팅·수익성 전략 보완 필요)을 받은 바 있음.
- 카카오톡 게시판 공지에 사용된 계정: `k9cjhmw7z9@privaterelay.appleid.com` (Apple 릴레이 이메일) — 이 볼트의 git 커밋에 붙는 계정태그 `[k9cjhmw7z9]`와 동일인으로 추정되어, 다중계정 동시작업 상황을 이해하는 단서가 됨.

<!-- ok -->
