# HANDOFF — 방역&클린 스킬 설계

> **[2026-07-12 갱신] 이 파일은 방역 설계 중심의 과거 HANDOFF입니다.**
> 최신 진행상황은 아래 세 곳을 먼저 보세요:
> - **범용 러너(④ 플랫폼 실행엔진) 구현·검증 완료** → [[ai공장짓기/runner/README|runner/README.md]]
>   (자체 14건 + 방역 연결 10건 + 콘텐츠 9건 테스트 전부 PASS, 방역 폴더 무수정)
> - **오류·발견사항 장부(하위모델 수리용)** → [[ai공장짓기/failure_log|failure_log.md]]
> - **콘텐츠 공장(주제→발행) 신축** → [[1. Projects/클로드 콘텐츠 ai/HANDOFF|1. Projects/클로드 콘텐츠 ai/HANDOFF.md]] (혜미 회신 대기 4건)
> 세션 상세: [[2. Areas/Claude 세션로그/2026-07-12|세션로그 2026-07-12]]


## ⚠ 지금 사용자 확인/회신이 필요한 것 (제일 중요 — 맨 위)

| 항목 | 무엇이 필요한지 |
|---|---|
| **골든셋 실제 사례** | `1. Projects/클로드 방역 ai/golden_set_template.yaml`을 채워서 회신 — 4개 유형(신규문의 2~3건, 정기방문리마인더 1~2건, 문서요청 1~2건, 무관한통화 1~2건) |
| **현장분리봇/견적금액산정봇/현장사진봇 필요 여부** | 위 템플릿 맨 아래 "추가_확인사항" 3개 질문(다중현장 빈도 / 표준가격표 여부 / 현장사진 촬영 여부) 답변 |
| **남은 구조변경 카드 5개 검토** (2026-07-07 갱신 — card-A는 Fable 5가 결정해 이번에 실제로 구현 완료. 남은 5개만 검토 필요) | `1. Projects/클로드 방역 ai/fable5_카드_사전정리_2026-07-07.yaml` 확인 후, 나머지 5개(고객현장분리/법정주기저장방식/AI전달필드최소화/권한체계/오프라인정책) 중 Fable 5에 상정할 게 더 있는지 알려주기 — 급한 것 없으면 골든셋 먼저 받아도 됨 |

---

## 전체 진행상황 요약표 (2026-07-07 기준 — 다른 대화에서도 이 표만 보면 파악 가능)

| 항목 | 상태 | 비고 |
|---|---|---|
| 방역 실제 업무 흐름 정리 | 완료 | 사업계획서 기준 6단계 + 리마인더 병행 트리거 확인 |
| 꽃집 14봇 재사용성 분석 | 완료 | 겉모양 재사용 약 64%, 부분재사용 포함 약 86% (추정치) |
| Fable 5 아키텍처 결정 | 완료 | triggers + run_if로 event/schedule 공존 해결, 승인받고 진행함 |
| v2 스키마 갱신 (triggers/run_if/entry_points) | 완료 | manifest.schema.v2.yaml에 3개 신규 개념 추가 |
| 방역 manifest.yaml 초안 | 완료 | 초안 당시 17 stage(local 10/model 6/human 1), 검증 통과 — **현재는 문서승인 stage 추가로 18 stage(아래 "문서승인워크플로우(card-A) 구현" 행 참고), 지금 최신 stage 수는 18** |
| validate_manifest.py | 완료 | entry_points 검사까지 포함, FAIL/WARN 0건 |
| 방문알림봇 이중 진입 문제 | 완료 | entry_points 필드로 해결, depends_on은 안 건드림 |
| SKILL.md | 완료 | 트리거/하는일/알려진한계 채움 |
| **파일 잘림 자동점검(verify_write.py)** | 완료 | Write/Edit 직후 자동 비교→다르면 자동 rename+재작성→HANDOFF.md 자동 기록까지 실제로 검증함 |
| **방역 run.py 뼈대** | 완료 | 작성 당시 17 stage 중 14개 실행 스텁 작성, 가짜 데이터로 처음부터 끝까지 통과(PASS) 확인, 조건분기(문서생성봇 on/off)도 양방향 확인 — **현재는 18 stage 전부(문서승인 포함) mock 구현 완료** |
| **골든셋 입력 템플릿** | 완료 | `golden_set_template.yaml` — 비개발자용 쉬운 안내문 포함 |
| 방역 골든셋(실제 사례) 수집 | **진행중 — 사용자 회신 대기** | 템플릿 드렸음, 채워서 주시면 반영 |
| 현장분리봇/견적금액산정봇/현장사진봇 on/off 확정 | **보류 — 사용자 회신 대기** | 골든셋 템플릿의 추가_확인사항 답변 필요 |
| 법정 문서 기재항목 / 권한체계 / 오프라인 범위 | **보류(미확정)** | 현재 설계 문서 어디에도 명시 안 됨 |
| 문의판정봇/보정봇 tier(sonnet) 실측 | **보류** | 골든셋 필요 |
| estimated_price(견적금액) 필요성 확인 | **보류** | 표준가격표 존재 여부 미확인(추가_확인사항 질문에 포함) |
| 만족도수집봇 타이밍 확정 | **보류** | 방문 직후 vs 시차 미정 |
| 실제 AI 모델 연동(STT/LLM 호출) | **미착수** | run.py는 전부 목업(mock) 스텁 — 골든셋 확보 후 실제 모델 호출로 교체 필요 |
| core+plugin 구조(3개 도메인 공용 엔진) 실제 구현 | **미착수** | 지금 run.py는 방역 전용 스크립트 — 꽃집/정부지원과 공유하는 범용 실행기 전환은 아직 |
| **새 문서(MVP 계획안) 법정요구사항 대조표** | 완료 | (a)기존구조로충분 7개 / (b)필드만추가 6개 / (c)구조변경검토 5개+추가4개 — `법정요구사항_대조표_2026-07-07.md` |
| **완료보고서·소독증명서 템플릿 3종** | 완료 | 템플릿A(간단형)/B(민원상세형)/소독증명서(법정9필드) — 전부 "실제 발급 금지" 경고문 포함, `templates/` 폴더 |
| **합성테스트 12건 실제 실행** | 완료 | `test_12cases_batch.py`로 run.py 재사용 실행 — 12건 모두 이벤트경로 끝까지 통과(PASS), 갭 3건 신규 발견(문서종류 미구분/승인상태 미확인/entry_points 미연결+리마인더 날짜로직 없음) |
| **권한·오프라인 설계메모** | 완료 | `설계메모_권한과오프라인정책.md` — 코드 미반영, 새 문서 제안과 현재 구조 차이만 정리 |
| **Fable 5 카드 사전정리(11개)** | 완료 | `fable5_카드_사전정리_2026-07-07.yaml` — 아직 Fable 5에 안 보냄, 사용자 승인 대기 |
| **문서승인워크플로우(card-A) 구현** | 완료 | Fable 5가 결정 → Sonnet이 구현. 승인블록 4상태/반려2갈래/발송차단 안전장치까지 실제 실행·검증. 11개 카드 중 3개(card-06/08/09) 코드로 해결 + card-A 구현완료로 카드 11→5개로 감소 |
| **템플릿 3종 — 사업자 정보 섹션 + 대조확인 경고문구 추가** | 완료 (2026-07-07 2회차) | 완료보고서 A/B, 소독증명서 템플릿에 상호명/대표자명/사업자등록번호 칸 + "국세청 공식 사업자등록증명과 대조 확인 필요" 경고문 신규 삽입. 기존 내용은 전부 그대로 유지 |
| **실행방법.md** | 완료 (2026-07-07 2회차, 신규 작성) | 1부 대표님용 사용안내 + 2부 개발자/다음세션용 기술안내로 구성. 실행 명령/핵심 메커니즘/남은 카드 5개/이번 전체점검 발견사항 3건 포함 |
| **최종요약보고서_2026-07-07.md** | 완료 (2026-07-07 2회차, 신규 작성) | 1부 대표님용 진행상황 요약 + 2부 정부지원사업 제출/멘토링용 요약으로 구성 |
| **전체 점검 (2회차)** | 완료 | validate_manifest.py/run.py 컴파일/12건 배치테스트 전부 재실행 → 전부 PASS. 템플릿-스키마 불일치 3건 발견(아래 상세) |

- 날짜: 2026-07-07
- 상태: **스키마 + manifest(18 stage) + SKILL.md + 자동점검도구 + run.py + 골든셋 템플릿 + 새문서 대조표/템플릿/12건테스트/설계메모/Fable카드 + 문서승인워크플로우(card-A)까지 전부 완료·검증(재실행 PASS 재확인, 2026-07-07).** 다음은 사용자가 (1)골든셋 (2)3개봇 여부 (3)남은 구조변경 카드 5개 검토 결과를 회신하는 것.

---

## 오늘 한 일 (시간순)
1. **재사용성 분석** — 방역 사업계획서(최최종제출본_2026년 혁신 소상공인 AI 활용지원 사업 신청서.pdf) 기반으로 실제 업무 흐름 검증 + 꽃집 14봇 재사용 표 작성. → `방역_업무흐름_및_꽃집재사용성_분석_2026-07-07.md`
2. **Fable 5 에스컬레이션 카드 작성** — architecture 후보 3개(스키마 확장/모듈분리/현상유지)를 카드 형식으로 정리해 전달(채팅 내 응답, 별도 파일 없음).
3. **Fable 5 결정 반영**:
   - `manifest.schema.v2.yaml`에 **0번 섹션(triggers)** 신규 추가 — event(문의접수)/schedule(방문주기) 두 트리거 타입을 선언. schedule의 실제 날짜확인은 스키마 밖 별도 소형 스케줄러 프로그램 책임으로 명확히 분리(스키마 안 무겁게 안 만듦).
   - `stage_fields`에 **run_if(조건부 실행)** 필드 추가. 조건이 거짓이면 stage를 건너뛰고 writes 필드는 비어있는 채로 다음 단계 진행.
   - open_issues 중 "조건부 stage 스킵 표현 수단 없음"과 "event/schedule 트리거 공존" 2건을 **[해결]**로 표시.
4. **방역 manifest.yaml 작성** (`클로드 방역 ai` 폴더, 꽃집 폴더는 안 건드림) — 17개 stage(local 10 / model 6 / human 1). 꽃집 14봇 대비 9개 겉모양 재사용, 2개 재설계(방문알림봇/문서생성봇), 3개 방역 전용 신규(현장완료입력봇/만족도수집봇/**리마인더봇**), 3개는 사용자 지시대로 run_if 자리만 만들고 on/off 확정 보류(현장분리봇/견적금액산정봇/현장사진봇).
5. **validate_manifest.py 신규 작성** (`ai공장짓기/scripts/validate_manifest.py`) — io/shared_context 정합성, depends_on/entry_stage 참조 무결성, run_if 문서화 관례를 검사. **방역 manifest.yaml 검증 결과: FAIL 0건 / WARN 0건 — 통과.**
6. **파일 동기화 버그 재발 확인** — 편집 중 여러 번 발생, `.stale-*`로 이름 바꾸고 새로 Write하는 방식으로 매번 복구.

## 신규 추가 vs 재사용 요약표

| 구분 | 내용 |
|---|---|
| **스키마 신규 추가** | triggers 섹션(0번), run_if 필드, entry_points 필드(stage_fields) — 셋 다 방역 요구사항 때문에 처음 생김 |
| **스크립트 신규 작성** | validate_manifest.py, verify_write.py, run.py |
| **문서 신규 작성** | SKILL.md, golden_set_template.yaml |
| **방역 stage 중 꽃집 겉모양 재사용(9개)** | 수집봇, 전사봇, 문의판정봇, 보정봇, 문의정리봇, 검수매니저, 대표자검수, 고객DB저장봇, 문자장부봇 |
| **방역 stage 중 재설계(2개)** | 방문알림봇(꽃집 dispatch_bot 백지재작성), 문서생성봇(꽃집 print_prep_bot 골격만 재사용) |
| **방역 전용 신규 stage(3개)** | 현장완료입력봇, 만족도수집봇, **리마인더봇**(schedule 트리거 진입점 — 이번 설계의 핵심) |
| **run_if로 자리만 마련, on/off 미확정(3개)** | 현장분리봇, 견적금액산정봇, 현장사진봇 — 골든셋 수집 후 결정 예정 |

## 확인 필요 항목 (임의 확정 안 함, manifest.yaml open_questions와 동일)
`1. Projects/클로드 방역 ai/manifest.yaml`의 `open_questions` 섹션 참고.

## 파일 위치
- 스키마(공용): `C:\Users\82106\Claude\Projects\ai공장짓기\manifest.schema.v2.yaml`
- 검증 스크립트: `C:\Users\82106\Claude\Projects\ai공장짓기\scripts\validate_manifest.py`
- 파일 잘림 자동점검 스크립트: `C:\Users\82106\Claude\Projects\ai공장짓기\scripts\verify_write.py`
- 재사용성 분석: `C:\Users\82106\Claude\Projects\ai공장짓기\방역_업무흐름_및_꽃집재사용성_분석_2026-07-07.md`
- 방역 manifest: `C:\Users\82106\Desktop\클로드 방역 ai\manifest.yaml`
- 방역 SKILL.md: `C:\Users\82106\Desktop\클로드 방역 ai\SKILL.md`
- 방역 run.py: `C:\Users\82106\Desktop\클로드 방역 ai\scripts\run.py`
- 방역 골든셋 입력 템플릿: `C:\Users\82106\Desktop\클로드 방역 ai\golden_set_template.yaml`
- 임시 테스트 입력(실제 골든셋 아님): `C:\Users\82106\Desktop\클로드 방역 ai\test\temp_mock_input_임시.json`
- 꽃집 참고 폴더(읽기 전용): `C:\Users\82106\Desktop\클로드 꽃집 ai`

---

## 2026-07-07 (이어서) — 1단계: 방문알림봇 이중 진입 문제 해결

**작업**: 방문알림봇이 event 경로(고객DB저장봇 이후)와 schedule 경로(리마인더봇 이후) 양쪽에서 진입 가능한 문제를, 사용자 지시대로 depends_on 그래프는 안 건드리고 새 필드 **entry_points**로 해결.

- `manifest.schema.v2.yaml` stage_fields에 `entry_points` 문서화 추가 — 형식: `["<trigger.type>:<식별자>", ...]`(event는 source, schedule은 interval). depends_on은 주 경로만 유지, entry_points가 대안 트리거 계통을 나열.
- 스키마 open_issues에 `[2026-07-07 해결]` 항목 1건 추가(entry_points로 이중 진입 문제 해결).
- `1. Projects/클로드 방역 ai/manifest.yaml`의 방문알림봇에 `entry_points: [event:inquiry, schedule:visit_cycle]` 적용, io_note 갱신, open_questions의 해당 항목을 `[해결]`로 표시.
- `validate_manifest.py`에 **3b번 검사** 추가 — entry_points 각 항목이 `type:식별자` 형식인지, triggers 섹션에 실제 선언된 트리거를 가리키는지 확인. 일부러 깨진 값(`event:존재안함`, `badformat`)으로 스모크 테스트해서 FAIL 2건이 정확히 잡히는 것 확인함(오탐 아님을 검증).
- **재검증 결과**: `1. Projects/클로드 방역 ai/manifest.yaml` → FAIL 0건 / WARN 0건, 통과.

**파일 동기화 버그 추가 재발**: `manifest.schema.v2.yaml`, `1. Projects/클로드 방역 ai/manifest.yaml`, `ai공장짓기/scripts/validate_manifest.py` 세 파일 모두에서 Edit 직후 truncation이 재현됨. 매번 `.stale-*`로 rename 후 Write로 복구. HANDOFF.md 자체도 여러 번 잘려서 rename+rewrite로 복구함.

## 2026-07-07 (이어서) — 2단계: 골든셋 수집 요청

사용자에게 실제 사례 제공을 요청함 — 4개 카테고리, 총 6~9건. (이후 3단계에서 정식 입력 템플릿 파일로 구체화됨 — `golden_set_template.yaml` 참고.)

## 2026-07-07 (이어서) — 3단계: SKILL.md 작성

`1. Projects/클로드 방역 ai/SKILL.md`를 새로 작성함(이전에 존재하지 않았음). 트리거 조건/하는 일/알려진 한계(3개 미정봇, 법정문서·권한체계·오프라인범위 미확정, 실행코드 없음)를 채움.

## 2026-07-07 (이어서) — 4단계: 전체 요약표 정리

HANDOFF.md 맨 위에 전체 진행상황 요약표(완료/진행중/보류/미착수) 추가.

---

## 2026-07-07 (다음 세션 이어서) — 1단계: 파일 잘림 자동점검 도구 (verify_write.py)

**배경**: 이번 세션에서 파일 truncation 버그가 4번 재현됨(스키마, 방역 manifest, validate_manifest.py, HANDOFF.md). 매번 사람이 wc -l/파싱으로 눈으로 확인하던 것을 스크립트로 자동화.

- `ai공장짓기/scripts/verify_write.py` 신규 작성 — 대상 파일과 "의도한 내용" 임시파일을 바이트 단위로 비교, 다르면 자동으로 `.stale-<타임스탬프>` rename 후 재작성하고 HANDOFF.md에 자동복구 로그 한 줄을 스스로 append함.
- 스모크 테스트 2건(단순 truncation 케이스, HANDOFF 로깅 케이스) 모두 정상 동작 확인.
- **실전에서 바로 적중**: SKILL.md의 "유지보수" 섹션 갱신(Edit) 직후 실제로 truncation이 재발했고, 이 스크립트로 처음 잡아서 자동 복구함 — 위 103번째 줄 근처의 "[자동복구 로그 2026-07-07 11:53]"이 이 스크립트가 스스로 남긴 기록.
- SKILL.md "유지보수" 섹션에 "2026-07-07부터 자동 점검으로 대응 중"이라고 갱신함(단, 진짜 후크가 아니라 "매번 호출하는 루틴"이라는 한계도 명시).
- **주의**: 완전 자동 후크(hook)는 아님 — Claude가 Write/Edit 직후 이 스크립트를 실제로 호출하는 루틴을 지키는 것에 의존함. 호출을 잊으면 무용지물이라는 한계를 스스로도 인지하고 있음.

## 2026-07-07 (다음 세션 이어서) — 2단계: 방역 run.py 뼈대 + 임시 데이터 테스트

- `1. Projects/클로드 방역 ai/scripts/run.py` 신규 작성 — manifest.yaml의 triggers/stages/depends_on/run_if/entry_points를 직접 읽어서 실행하는 범용 골격(위상정렬 기반 실행기). 정부지원사업/꽃집의 run.py는 도메인 특화 하드코딩 스크립트라 구조 참고만 하고(CLI 진입점, 입력파일 인자, 결과 JSON 출력 등 관례), manifest 기반 실행기 자체는 새로 설계함.
- 17개 stage 중 14개(리마인더봇 제외 — schedule 전용, 이번 event 테스트에서는 대상 아님)에 목업(mock) 함수 작성. 실제 STT/LLM 호출 없음 — golden_set 확보 후 교체 필요한 부분으로 명시.
- 현장분리봇/견적금액산정봇/현장사진봇 3개는 `run_if: 미정_골든셋수집후결정` 조건을 "기본 False(건너뜀)"로 평가하도록 구현.
- **테스트 실행 결과**: 가짜 예시 데이터(`test/temp_mock_input_임시.json`, "바퀴벌레 방역 문의 + 소독증명서 요청"을 가정한 가상의 카카오톡 문의) 1건으로 실행 → 13개 stage 실행, 3개 skip, **"PASS: 파이프라인이 처음부터 끝까지(만족도수집봇까지) 통과했습니다"** 확인.
- 추가로 document_requested=False로 바꾼 변형 테스트도 실행해 문서생성봇의 run_if 조건부 분기(고객이_문서를_요청함)가 양방향(실행/스킵) 모두 정상 동작하는 것을 확인함.

## 2026-07-07 (다음 세션 이어서) — 3단계: 골든셋 입력 템플릿 작성

- `1. Projects/클로드 방역 ai/golden_set_template.yaml` 신규 작성 — 비개발자(대표님) 기준으로 쉬운 말 안내문과 빈칸을 채워넣음. 4개 유형(신규_문의_사례/정기방문_리마인더_사례/문서요청_사례/무관한_통화_사례) + 추가_확인사항(3개 미정 봇 판단 근거 질문) 구성.
- 파일 맨 끝에 "내부 참고용" 섹션을 별도로 분리해, 각 항목이 나중에 manifest.yaml의 shared_context 어디로 매핑되는지만 개발자 참고용으로 남겨둠(사용자가 안 봐도 되는 부분임을 명시).

## 2026-07-07 (다음 세션 이어서) — 4단계: 진행상황 정리

- HANDOFF.md 맨 위에 "⚠ 지금 사용자 확인/회신이 필요한 것" 표를 새로 추가해 골든셋 실 사례와 3개 봇 필요여부 판단 요청을 가장 먼저 보이게 함.
- 전체 진행상황 요약표에 이번 라운드 3개 항목(파일 잘림 자동점검, run.py 뼈대, 골든셋 템플릿)을 완료로 추가하고, "실행 코드 작성"을 미착수→완료(스켈레톤 기준)로 갱신, 대신 "실제 AI 모델 연동"과 "core+plugin 공용 엔진 구현"을 남은 미착수 항목으로 명확히 분리함.

**막히거나 스키마를 또 바꿔야 할 것 같은 상황은 이번 1~4단계 작업 중 발생하지 않았음** — 전부 이미 지시받은 설계를 그대로 구현한 것이라 별도 승인 질문 없이 진행함.

---

## 2026-07-07 (새 문서 반영) — 새 문서 "방역&클린 모바일 AI 운영관리 MVP 계획안" 반영 작업 (1~6단계)

**배경**: 사용자가 `클로드 방역 ai` 폴더에 새 문서(법정서식/보존기간/권한체계/오프라인정책/합성테스트12건이 담긴 MVP 심층 리서치 보고서)를 추가로 넣어줌. 6단계 순서로 반영 지시.

### 1단계 — 법정요구사항 vs 기존설계 대조표
`법정요구사항_대조표_2026-07-07.md` 신규 작성.
- (a) 기존 구조 그대로 반영 가능: 오더메모/고객DB/D-day리마인더(자리)/문서초안생성/대표자승인/만족도수집 등 7개
- (b) 필드만 추가하면 되는 것: 소독증명서 9개 법정필드, 사용약품명, 사진개수/설명, 보관위치·보존기간, 반려사유, 감사로그 상세화 등 6개 — 전부 구조는 안 바꿔도 되는 수준
- (c) 구조 변경이 필요할 수도 있는 것: 사용자가 지목한 5개(고객/현장분리, 법정주기저장방식, 승인이력, 템플릿버전, AI전달필드최소화)에 각 1~2문장 근거만 작성 — **결정 안 함**. 3단계 테스트에서 추가로 4개 후보(문서요청단일boolean, 승인상태4종, 권한체계, 오프라인정책) 더 발견됨.

### 2단계 — 법정 문서 템플릿 3종 작성
`templates/` 폴더 신규 생성.
- `완료보고서_템플릿A_간단형.md`, `완료보고서_템플릿B_민원대응상세형.md`, `소독증명서_템플릿.md`(법정 9개 필수항목: 상호/실시면적/소재지/관리자확인/소독기간/소독내용종류/약품사용내용/발급일/소독실시자)
- 3개 파일 전부 "⚠️ 법적 필수항목 확인 전 — 실제 발급 금지, 초안 검토용" 경고문 + 법령 원문 미검증 안내 포함.
- **참고**: "지난번에 만들어둔 초안 양식 2개"라는 사용자 언급과 달리, 실제로는 이 폴더에 기존 템플릿 파일이 없었음 — 새 문서 기준으로 처음 작성함(착오였을 수도, 다른 세션/도구에서 만든 것일 수도 있어 확인 필요하면 알려주세요).

### 3단계 — 합성 테스트 12건 실제 실행
`test/새문서_합성테스트_12건.yaml`(새 문서의 가상 데이터 12건 변환) + `scripts/test_12cases_batch.py`(run.py를 재작성 없이 그대로 불러와 실행하는 배치 테스트) 신규 작성. 실행 로그: `test/12건_배치실행_결과_2026-07-07.log`. 결과 요약: `3단계_12건테스트_결과요약.md`.
- 12건 전부 event(문의접수) 경로로는 끝까지(만족도수집봇까지) 정상 통과(PASS).
- **갭 3건 신규 발견(고치지 않고 목록에만 추가)**:
  1. 문서요청이 단일 boolean이라 완료보고서만/소독증명서만/둘다 요청을 구분 못함(document_draft 필드 구성이 12건 전부 동일).
  2. 승인상태 4종(승인완료/반려/승인대기/초안)을 표현 가능한지 검증 못함 — 테스트모드 대표자검수가 항상 자동승인이라.
  3. entry_points가 실제 run.py 실행기 그래프(depends_on 기반)에는 연결 안 돼 있어 schedule 트리거 실행 시 리마인더봇에서 멈춤 + 리마인더봇 자체가 날짜 계산을 안 하는 빈 스텁이라 D-day/지연 개념이 아예 없음.

### 4단계 — 권한·오프라인 설계 메모
`설계메모_권한과오프라인정책.md` 신규 작성. 새 문서의 권한모델(현장입력자/대표자/관리자)과 오프라인정책(임시저장/충돌처리/잠금/감사로그)을 옮겨 적고 현재 구조와의 차이만 표시. **코드/manifest에는 반영 안 함** — 설계 메모로만 존재.

### 5단계 — Fable 5 카드 사전정리
`fable5_카드_사전정리_2026-07-07.yaml` 신규 작성. 1단계+3단계+4단계에서 나온 구조변경 후보 11개를 cards(id/type/summary/evidence/confidence) 형식으로 정리. **아직 Fable 5에게 보내지 않음** — should_escalate_to_fable: false로 명시, 사용자 승인 대기 중.

### 6단계 — 진행상황 정리
- 위쪽 "⚠ 지금 사용자 확인/회신이 필요한 것" 표에 "신규 구조변경 카드 11개 검토" 행 추가.
- "전체 진행상황 요약표"에 이번 5개 산출물(대조표/템플릿/12건테스트/설계메모/Fable카드) 완료로 추가.

**막히거나 스키마를 실제로 바꾼 적은 없음** — 1단계 대조표와 5단계 카드는 전부 "후보와 근거만" 정리했고, 어떤 스키마/구조 변경도 사용자 승인 없이 결정하지 않음.

---

## 2026-07-07 (새 세션 이어서) — 필드 추가 6개 반영 + 갭 3개 재분류 + 카드 11개→6개 정리

**배경**: 새 세션 시작, 이 HANDOFF.md를 먼저 읽고 진행상황 파악한 뒤 사용자가 지시한 4단계(필드 6개 반영 → 갭 3개 재분류 → 최종 카드 정리 → HANDOFF 갱신) 순서로 진행함. `클로드 방역 ai` 폴더가 이번 세션에 새로 연결돼야 해서 폴더 접근 요청부터 시작함.

### 1단계 — 필드만 추가 6개 반영 (구조 변경 아님)
`법정요구사항_대조표_2026-07-07.md`의 (b)그룹 6개를 `1. Projects/클로드 방역 ai/manifest.yaml`에 전부 반영:
1. 소독증명서 법정 9개 필드(상호/실시면적/소재지/관리자확인/소독기간/소독내용종류/약품사용내용/발급일/소독실시자) → `document_draft.certificate` 하위 properties로 추가
2. 사용 약품명 → `used_chemicals` 신규 필드(현장완료입력봇이 씀, 문서생성봇이 읽음)
3. 사진 개수·설명 → `site_photo`를 문자열(uri) 하나에서 배열(`[{uri, description}]`)로 확장
4. 문서 잠금/보관위치/보존기간 → `document_draft` 하위에 `locked`/`storage_location`/`retention_period` 추가
5. 반려사유 → `rejection_reason` 신규 필드 + `approval_action` enum에 "반려" 값 추가(대표자검수 io.writes, 고객DB저장봇 io.reads 갱신)
6. 감사로그 상세화 → `history_log` 설명 확장(writer는 문자장부봇 하나로 유지 — 여러 stage가 실시간으로 append하는 건 구조변경 범주라 지금은 안 함, 그 한계를 필드 설명에 명시)

**재검증**: `validate_manifest.py` 실행 → FAIL 0건 / WARN 0건, 통과. (중간에 Edit 직후 YAML이 두 번 truncation돼서 매번 `.stale-*`로 rename 후 Write로 전체 재작성해 복구함 — 이번 세션에서도 파일 동기화 버그가 그대로 재현됨.)

### 2단계 — 갭 3개 재분류 (3단계 12건 테스트에서 발견한 갭)
1. **문서종류구분 불가(완료보고서/소독증명서/둘다)** → **필드만 추가로 해결**. `document_requested` 단일 boolean을 `report_requested`/`certificate_requested` 2개로 분리, `document_draft`를 `report`/`certificate` 하위 객체로 재구성. `문서생성봇`의 run_if 조건도 `완료보고서_또는_소독증명서를_요청함`으로 갱신(기존 `고객이_문서를_요청함`은 구버전 호환용으로 run.py에 남겨둠).
2. **승인상태 4종(승인완료/반려/승인대기/초안) 확인 안 됨** → **구조 변경 필요로 판정, 카드로 이관**. 이유: 이 4단계 상태는 "고객 문의 승인"이 아니라 **완료보고서·소독증명서 문서 자체**의 초안→승인대기→승인/반려 워크플로우인데(MVP 문서의 승인 워크플로우 flowchart·템플릿 "작성상태: 초안/승인대기/승인/반려" 항목 재확인으로 근거 명확해짐), 지금 구조에는 이 문서를 승인하는 human 게이트가 아예 없음 — 기존 `대표자검수`는 문서생성봇보다 앞에 있어 "방문확정"만 승인하고 문서 자체는 아무도 승인 안 한 채 문자장부봇으로 바로 감. `approval_action` enum만 늘리는 걸로는 안 풀림(별개의 게이트가 필요) → 새 human stage(가칭 "문서승인봇")가 필요할 수 있다고 판단, 카드로 이관.
3. **정기방문 알림 연결 안 됨 + 날짜계산 없음** → **필드/실행기 구현으로 해결**(스키마 구조는 안 바꿈). 원인 2가지 모두 "이미 있는 entry_points 방식을 실행기가 아직 안 따라간 것"과 "이미 있는 리마인더봇 stage에 실제 로직이 없던 것"이라 구조변경이 아니라고 판단:
   - `scripts/run.py`의 `build_graph()`가 entry_points를 실제로 반영하도록 수정(예전엔 depends_on만 봐서 schedule 경로가 리마인더봇에서 멈췄음) — `topo_order()`도 depends_on을 다시 읽지 않고 build_graph가 만든 그래프를 그대로 재사용하도록 같이 고침(안 그러면 위상정렬이 꼬임).
   - 리마인더봇 mock에 실제 날짜비교 로직 구현(`last_visit_date` + `visit_cycle`(문자열→일수 매핑, 격월/분기/반기/연1회) → `due_date`/`days_until_due`/`is_overdue` 계산). `reminder_due_list` shared_context 필드도 이 3개 하위 필드를 갖도록 확장.
   - 방문주기가 도래한 고객이 여러 명일 때는 병렬이 아니라 **manifest.schema.v2.yaml#5의 기존 "묶음 순차 처리 정책"을 그대로 재사용**해 1명씩 끝까지 처리(새 문법 발명 안 함).

### 3단계 — 배치 테스트로 검증
`test/temp_schedule_mock_input_임시.json` 신규 작성(가상 고객 3명 — 지연 1명/임박 1명/미도래 1명) + `PEST_SKILL_TODAY` 환경변수로 오늘 날짜 고정 가능하게 함(정부지원 스킬의 `GOV_SKILL_TODAY` 패턴과 동일). event/schedule 둘 다 단독 실행 확인 후, `test_12cases_batch.py`도 새 필드명(report_requested/certificate_requested)과 실제 날짜비교가 가능한 schedule 더미 입력으로 갱신해 재실행:
- **검증1(문서종류구분)**: 12건 전부 원본과 정확히 일치 → **해결 확인**(card-06 해결)
- **검증2(승인상태4종)**: 여전히 `방문확정` 하나로 수렴 → **갭 유지**(구조변경 카드로 이관, 예상대로)
- **검증3(entry_points+날짜계산)**: schedule 트리거가 리마인더봇 이후 6개 stage까지 실제로 이어지고, 11/12건이 실제 날짜비교로 due_list에 정확히 잡힘 → **해결 확인**(card-08, card-09 해결)

(중간에 `scripts/run.py`가 세 번 truncation돼서 — 한 번은 Write 도중 내가 실수로 파일을 짧게 잘라 보낸 것, 두 번은 환경 자체의 파일 동기화 버그 — bash에서 heredoc(`cat > file << EOF`)으로 직접 써서 복구함. 이번에 새로 알게 된 것: **Windows쪽 Write/Edit 도구로 쓴 내용이 bash(Linux 샌드박스)에 반영되기까지 지연이 있고, 심하면 몇 분이 지나도 bash 쪽에서 옛날 버전이 계속 보일 수 있음** — 이럴 땐 `.stale-*` rename+재시도보다 bash heredoc으로 직접 쓰는 게 더 빠르게 해결됨. 반대 방향(bash에서 쓴 내용 → Read 도구로 확인)은 즉시 반영되는 것도 확인함.)

### 4단계 — 최종 카드 정리 (11개 → 6개)
`fable5_카드_사전정리_2026-07-07.yaml` 갱신:
- **해결로 제외(3개)**: card-06(문서종류구분불가), card-08(entry_points실행기미연결), card-09(리마인더날짜로직없음) — 전부 위 2~3단계에서 실제로 코드 반영 후 테스트로 검증함.
- **병합(3개→1개)**: card-03(승인이력) + card-04(템플릿버전) + card-07(승인상태4종불확실) → **card-A(문서승인워크플로우)** 하나로 통합. 셋 다 "문서 자체의 초안→승인 워크플로우가 없다"는 같은 근본 문제였음. confidence 0.9(3단계 테스트로 실제 재현 + MVP 문서 근거 명확).
- **유지(5개)**: card-01(고객현장분리, 0.6) / card-02(법정주기저장방식, 0.3) / card-05(AI전달필드최소화, 0.4) / card-10(권한체계3역할vs현재1개, 0.5) / card-11(오프라인정책부재, 0.8).

**최종 카드 6개 표** (사용자 검토용):

| 카드 | 한 줄 요약 | confidence |
|---|---|---|
| card-A 문서승인워크플로우 | 완료보고서·소독증명서 "문서 자체"의 초안→승인대기→승인/반려 게이트가 지금 구조엔 없음(대표자검수는 문서 생성 전 단계라 문서를 승인 안 함) | 0.9 |
| card-11 오프라인정책부재 | 오프라인 임시저장·동기화·충돌처리 개념이 manifest/run.py 어디에도 없음 | 0.8 |
| card-01 고객현장분리 | customer_record가 고객+현장을 한 object로 뭉쳐 저장 — 고객 1명·현장 여러 곳/여러 방문이력 표현 못할 수 있음 | 0.6 |
| card-10 권한체계3역할vs현재1개 | 현장입력자/대표자/관리자(3~4역할) 구분 필요할 수 있는데 지금은 대표자검수(human) 1개뿐 | 0.5 |
| card-05 AI전달필드최소화 | 일부 stage가 원본 필드를 폭넓게 읽음 — 최소수집·목적제한 원칙 지키려면 접근범위 재설계 필요할 수 있음 | 0.4 |
| card-02 법정주기저장방식 | visit_cycle이 문자열 하나뿐 — 시설유형별 법정주기가 다르면 매핑구조 필요할 수 있음(원문 미확인) | 0.3 |

### 진행상황 표 갱신
위쪽 "⚠ 지금 사용자 확인/회신이 필요한 것" 표의 "신규 구조변경 카드" 행을 11개→6개로 갱신함(다른 대기 항목 — 골든셋 실 사례, 3개봇 여부 — 은 그대로 유지, 아직 회신 없음).

**구조를 실제로 바꾸는 결정은 이번에도 내리지 않음** — 1~3단계는 전부 "이미 승인된 run_if/entry_points 방식을 재사용한 필드/구현 추가"였고(사용자가 미리 "구조 아니니 바로 반영해도 된다"고 지시한 범위), 진짜 구조변경 후보(카드 6개)는 여전히 사용자 검토 대기 중.

---

## 2026-07-07 (같은 날, 다음 세션) — card-A(문서승인워크플로우) 구현: Fable 5 결정 실제 반영

**배경**: 사용자가 Fable 5에게 card-A(문서승인워크플로우) 구조를 직접 상정해 결정을 받아옴 — 결정 내용은 `decision-log_skill-factory-architecture.md` 7절에 이미 상세 기록돼 있었음(별도 세션/도구에서 기록된 것으로 보임). 이번 세션은 그 결정을 실제 스키마/manifest/코드에 반영하는 작업.

### 반영 내용
1. **`manifest.schema.v2.yaml`**: `check_output_schema.approval_block` 신규(status/version/approved_by/approved_at/rejection_reason/rejection_target/version_history — 도메인 무관 범용 패턴). `stage_fields.on_fail.human_reject.return_to`를 단일값(기존, 하위호환)과 매핑(object, rejection_target별 라우팅) 둘 다 지원하도록 확장.
2. **`1. Projects/클로드 방역 ai/manifest.yaml`**: `document_draft.report`/`certificate` 각각에 `approval_required`+`approval` 블록 추가. 기존 document_draft 최상위 단일 `locked` 필드는 폐기하고 report/certificate 각자의 `locked`로 이동(문서 종류별 독립 잠금이 필요해서 — 예: 완료보고서는 승인, 소독증명서는 반려인 케이스를 표현하려면 공유 필드로는 안 됨). 새 stage `문서승인`(kind: human, run_if: 승인필수_문서가_존재함) 추가 — 문서생성봇 뒤·문자장부봇 앞. `문자장부봇.depends_on`을 `[현장완료입력봇]`→`[문서승인]`으로 바꿔 DAG 자체가 "발송은 승인 게이트 통과 후"를 강제하도록 함. summary: human 1→2개, stage 총 17→18개.
3. **`scripts/run.py`**: 문서생성봇이 반려 후 재생성 시 version+1하고 반려 이력을 version_history에 보존(삭제 안 함)하도록 갱신. `run_문서승인` mock 신규(테스트용 `문서승인_시뮬레이션` 입력값으로 승인/반려 흉내, 리스트로 주면 재시도마다 순차 소비 — 반려 후 재승인 시나리오 테스트 가능). `_run_document_approval_cycle()` 신규 — 문서생성봇↔문서승인을 반려-재작업 루프로 묶어 **명령형(imperative)으로 처리**(depends_on 그래프에 순환을 만들지 않음 — return_to는 그래프 엣지가 아니라 메타데이터라는 기존 스키마 원칙 그대로 따름). rejection_target="표현오류"→문서생성봇만 재실행, "데이터오류"→현장완료입력봇으로 되돌아가 데이터 재입력 후 문서생성봇 재실행.
4. **가장 중요한 안전장치**: `run_문자장부봇`에 "승인완료 아닌 문서는 절대 발송 안 함" 철칙을 강제 구현 — `approval_required=true`인 문서 중 `approval.status != 승인완료`인 게 하나라도 있으면 `RuntimeError`를 던져 발송 자체를 차단. 파이프라인 순서(문서승인이 먼저 온다는 것)만 믿지 않고 발송 stage 자체에서 다시 검증(이중 방어).
5. **스키마 레벨엔 방역 전용 단어를 넣지 않음** — "소독증명서" 개념은 스키마가 아니라 방역 manifest 인스턴스에서 `certificate.approval_required: true`로만 표현. 완료보고서도 MVP 문서 근거(대표자 승인 전 발송 금지가 두 템플릿 모두에 명시)로 `approval_required: true`로 설정.

### 검증
- `validate_manifest.py`: FAIL 0 / WARN 0, 통과.
- `test_12cases_batch.py` 갱신 후 재실행:
  - 기본 12건(자동승인) 전부 문서 승인상태=승인완료로 정상 도달.
  - **추가 시나리오 A**(표현오류 반려 1회→재승인): PASS — 문서생성봇 2회 실행, version 1→2, 최종 승인완료.
  - **추가 시나리오 B**(데이터오류 반려 1회→현장완료입력봇 복귀→재승인): PASS — 현장완료입력봇 2회 실행 확인(데이터 재입력 경유), version 1→2, 최종 승인완료.
  - **추가 시나리오 C**(가장 중요 — 지속 반려→재시도 3회 소진→발송 시도): PASS — `RuntimeError: [안전장치 발동 - 발송 차단]`이 실제로 발생해 미승인 문서 발송을 막는 것을 확인.
- card-A(및 병합 전이던 card-03/04/07) 해결 확인.

### 파일 동기화 버그 계속 재현
이번 라운드에서도 `manifest.yaml`, `run.py`(2번) truncation이 재현됨 — 매번 Read 도구로 실제 온전한 내용 확인 후 bash heredoc(`cat > file << EOF`)으로 직접 재작성해 복구. Windows쪽 Write/Edit 결과가 bash(Linux 샌드박스) 쪽에 반영되기까지 지연이 있고, bash에서 직접 쓰면 즉시 반영된다는 점(지난 세션에 이미 확인된 패턴)을 다시 활용함.

### decision-log / HANDOFF 갱신
- `decision-log_skill-factory-architecture.md` 7.4 체크리스트 4개 전부 완료 표시, 7.5절에 실행 결과 요약 추가.
- 위쪽 "⚠ 사용자 확인/회신이 필요한 것" 표의 카드 검토 항목을 "6개"→"card-A 완료, 남은 5개"로 갱신.

### 남은 미결정 사항 (임의 확정 안 함)
- 반려 재시도 최대 횟수(현재 mock은 3회로 임의 설정) — 실제 운영에서 몇 회까지 자동 허용할지 미정.
- version_history 보존 형식·기간이 문서 자체 법정 보존기간(2년, 잠정값)과 같아야 하는지 미확인.
- 두 항목 모두 `1. Projects/클로드 방역 ai/manifest.yaml`의 open_questions에 기록해둠.

**구조 변경(card-A)은 이번엔 사용자가 Fable 5로부터 직접 받아온 결정을 그대로 구현한 것** — Claude가 스스로 판단해서 실행하지 않았음(정책 준수). 남은 5개 카드는 여전히 사용자 판단 대기.

---

## 2026-07-07 (같은 날, 3번째 세션) — 전체 점검 + 템플릿 사업자정보 보완 + 실행방법.md/최종요약보고서 신규 작성

**배경**: 사용자가 "지난번에 시킨 4단계(전체 점검, 템플릿 안내문 다듬기, 실행방법.md 최신화, 최종 요약보고서)를 그대로 진행해달라"고 요청. 이 프로젝트 안에서 그 "지난번 4단계" 지시 자체(실행방법.md, 최종 요약보고서 파일)를 찾을 수 없었음 — `실행방법.md`도 최종 요약보고서 파일도 이 폴더엔 없었고, 템플릿에도 사용자가 있다고 생각한 "사업자 정보" 항목이 없었음. 착오였을 가능성이 있어 3가지를 먼저 확인 질문으로 물어봤고(실행방법.md 대상/최종요약보고서 목적/사업자정보 섹션 신규 추가 여부), 사용자가 전부 "둘 다(섹션 나눠서)" / "네, 이번에 새로 추가"로 답해 아래 내용을 새로 만듦.

### 1. 템플릿 3종에 "사업자 정보" 섹션 + 대조확인 경고문구 신규 추가
`완료보고서_템플릿A_간단형.md`, `완료보고서_템플릿B_민원대응상세형.md`, `소독증명서_템플릿.md` 3개 전부에 "용도"(또는 상응 위치) 바로 아래, "기본 정보"류 섹션 바로 위에 아래 내용을 삽입:
```
## 사업자 정보 [2026-07-07 신규 추가]
- 상호명:
- 대표자명:
- 사업자등록번호:

⚠️ 이 사업자 정보는 실제 발급 전, 국세청에서 발급하는 공식 사업자등록증명(사업자등록번호 등)과 반드시 대조 확인해야 합니다. 임의로 입력된 값이 아닌지 확인하는 절차가 필요합니다.
```
(경고 문구는 사용자가 지정한 문장 그대로, 한 글자도 안 바꿈.) 소독증명서 템플릿은 이미 "상호"라는 법정 필드가 별도로 있어서, 혼동 방지용으로 "법정 필수항목 1번 상호는 이 사업자 정보의 상호명과 같은 값이어야 한다"는 참고문 한 줄만 추가로 덧붙임(사용자가 요청한 문구는 아니고, 다듬기 범위 내 판단으로 추가 — 필요 없으면 알려주면 뺌). **그 외 기존 내용(경고문/문서번호/작성상태/문서버전/발송금지 문구, 기존 섹션들)은 전부 한 글자도 안 건드림** — Read 도구로 삽입 전후 라인별 비교해서 확인함.

### 2. 전체 점검 (validate_manifest.py / run.py / 배치테스트 / 템플릿-스키마 일치 재확인)
전부 재실행해서 확인:
- `validate_manifest.py "1. Projects/클로드 방역 ai/manifest.yaml"` → FAIL 0 / WARN 0, PASS.
- `manifest.schema.v2.yaml` YAML 파싱 정상, `approval_block` 존재 확인.
- `python3 -m py_compile scripts/run.py` → 정상.
- `test_12cases_batch.py` 재실행 → 검증1(문서종류구분)/검증2(승인워크플로우 A·B·C 시나리오)/검증3(entry_points+날짜계산) 전부 "[해결됨]", 전체 결과 PASS.

**발견사항 3건(구조 변경 아님, 참고용 — 실행방법.md 2부에도 동일하게 기록)**:
1. 템플릿의 "작성상태" 표기가 "초안/승인대기/**승인**/반려"로 돼있는데, 스키마·manifest의 실제 enum은 "초안/승인대기/**승인완료**/반려"임 — 사람이 읽는 문서라 당장 문제는 아니지만 용어 표기가 다름.
2. 이번에 템플릿에 추가한 "사업자 정보"(상호명/대표자명/사업자등록번호)는 문서 서식에만 있고, `manifest.yaml`의 shared_context나 document_draft 스키마에는 대응 필드가 아직 없음 — 지금은 사람이 직접 채우는 칸. 나중에 고객DB에서 자동으로 채우려면 필드 추가(구조 변경)가 필요함.
3. `SKILL.md`의 "실행 방법"/"알려진 한계" 절이 "아직 실행 가능한 코드가 없음"이라고 돼있는데, 지난 세션들에서 `run.py`+`test_12cases_batch.py`가 이미 만들어지고 통과함 — 이번 요청 범위 밖이라 SKILL.md는 건드리지 않았고, 다음에 갱신 필요함만 기록.

### 3. `실행방법.md` 신규 작성 (`1. Projects/클로드 방역 ai/실행방법.md`)
1부(대표님용 — 문서 흐름/안전장치/사업자정보 새 주의사항/지금 안 되는 것) + 2부(개발자·다음 Claude 세션용 — 파일 구성/실행 명령/핵심 메커니즘/동기화 버그 대응법/남은 카드 5개/이번 전체점검 발견사항 3건)로 섹션을 나눠 작성.

### 4. `최종요약보고서_2026-07-07.md` 신규 작성 (`1. Projects/클로드 방역 ai/최종요약보고서_2026-07-07.md`)
1부(대표님용 — 이번에 뭐가 새로 됐는지, 확인 결과, 아직 사람이 결정할 5가지, 아직 확정 안 된 것) + 2부(정부지원사업 제출/멘토링용 — 사업아이템 요약, 구현·검증 범위 표, 미확정 항목, 성과지표, 멘토링 점검포인트)로 섹션을 나눠 작성. 2부는 `방역&클린 모바일 AI 운영관리 MVP 계획안.md`의 기존 승인된 문안(사업아이템 한줄요약/KPI표)을 그대로 재사용하고 임의로 새 문구를 만들지 않음.

### 이번 회차에서 새로 만든 파일
- `1. Projects/클로드 방역 ai/실행방법.md` (신규)
- `1. Projects/클로드 방역 ai/최종요약보고서_2026-07-07.md` (신규)
- 템플릿 3종은 기존 파일에 섹션만 추가(덮어쓰지 않음)

### 카드 개수 변동 없음
이번 회차는 구조 변경을 하나도 하지 않음(점검·문서화·서식 보완만) — 구조변경 후보 카드는 여전히 5개(card-01/02/05/10/11), 그대로 사용자 판단 대기.

---

## 2026-07-07 (같은 날, 4번째 세션) — 발견사항 3건 해결 + 골든셋 필드 보강 + 문서 간 말맞추기 + 대표님용 현황보고 1장

**배경**: 직전 라운드에서 남긴 "발견사항 3건"을 전부 해결해달라는 사용자 지시 + golden_set_template.yaml 필드 보강 + 문서 4종(HANDOFF/SKILL.md/최종요약보고서/실행방법.md) 상호 불일치 정리 + 대표님용 1장짜리 현황보고 작성. 명시적 전제: "전부 구조를 새로 바꾸는 게 아니라 이미 있는 것들끼리 말을 맞추는 작업" — 실제로 구조(그래프/stage 종류) 변경은 없었고, 필드 추가와 문서 갱신만 수행함.

### 1단계 — 발견사항 3건 전부 수정
1. **템플릿 상태값 통일**: 완료보고서 A/B, 소독증명서 템플릿 3종의 "작성상태" 줄을 "초안 / 승인대기 / **승인완료** / 반려"로 수정(기존 "승인" → "승인완료"). 나머지 상태값(초안/승인대기/반려)과 다른 "승인" 표현(예: "대표자 승인 여부", "검수 및 승인")은 enum이 아니라 일반 라벨이라 그대로 유지 — grep으로 3개 파일 전체 재확인함.
2. **사업자 정보 필드를 manifest 데이터모델에 연결**(필드만 추가, 구조 변경 아님): `manifest.yaml`의 `shared_context`에 `business_info`(상호명/대표자명/사업자등록번호) 신규 추가. `현장완료입력봇.io.writes`에 추가(사용자가 지정한 위치 그대로), `문서생성봇.io.reads`에 추가해 `document_draft.report`/`certificate` 양쪽에 복사, `certificate.상호`는 `business_info.상호명`으로 자동 채우도록 함(중복 입력 방지). `scripts/run.py`에도 `DEFAULT_BUSINESS_INFO` 상수 + `run_현장완료입력봇`/`run_문서생성봇` 갱신으로 실제 저장→전달 구현, 배치 테스트 로그에 `business_info={...}` 값이 실제로 찍히는 것으로 검증함.
3. **`SKILL.md` "실행코드 없음" 문구 갱신**: "실행 방법"/"알려진 한계" 절을 다시 써서 `run.py`(mock 실행기)가 실제로 존재하고 18 stage + 문서승인워크플로우까지 동작한다는 점, 실행 명령어, 남은 한계(실제 AI 모델 미호출/golden_set 없음/카드 5개 등)를 반영.

**재검증**: `validate_manifest.py`(FAIL 0/WARN 0) + `run.py` 컴파일 + `test_12cases_batch.py`(검증1/2/3 전부 [해결됨], 전체 PASS) 재실행 — 전부 통과 확인.

**파일 동기화 버그 재발 및 대응**: 이번 라운드에서도 `run.py`, `manifest.yaml` 둘 다 Edit 직후 bash 쪽에서 truncation/stale 증상이 재현됨(Windows Read 도구는 항상 정상이었음). 매번 `.stale-<timestamp>`로 rename 후 bash heredoc으로 전체 재작성해 복구. **한 가지 사고 기록**: `HANDOFF.md`를 heredoc으로 재작성하던 중 델리미터 불일치로 실제로 파일이 깨지는 사고가 있었음(이번엔 환경 버그가 아니라 heredoc 작성 실수) — 즉시 `.stale-*-corrupted`로 rename 후 Write 도구로 Read 확인된 정확한 내용을 다시 써서 복구함. 이후로는 큰 파일 heredoc 작성 시 델리미터를 더 특이한 문자열로 쓰고 작성 직후 반드시 파싱 검증하는 것으로 유의.

### 2단계 — golden_set_template.yaml 필드 보강
"문서요청_사례" 항목에 3개 필드 추가: `이_문서에_들어간_사업자_정보_상호명_대표자명_사업자등록번호`, `보내기_전에_대표님이_확인_승인_하셨나요`, `한_번에_확정됐나요_아니면_고치고_다시_보낸_적_있나요`(반려 이력·사유 관련). 내부 참고용 매핑 주석도 갱신 — 예전 `document_requested` 단일필드 언급을 `report_requested`/`certificate_requested`로 교체하고, `business_info`/`approval.status`/`approval.rejection_reason`/`rejection_target` 매핑 설명 추가.

### 3단계 — 문서 4종 상호 일관성 점검 및 통일
- **stage 개수 표기(17 vs 18) 불일치 발견 및 수정**: `HANDOFF.md` 상단 "전체 진행상황 요약표"의 "방역 manifest.yaml 초안"/"방역 run.py 뼈대" 두 행이 작성 당시 그대로 "17 stage"라고 남아있어, 이후 다른 곳(문서승인 추가로 18개가 됐다는 설명)과 불일치하는 것으로 읽힐 수 있었음 — 각 행에 "초안/작성 당시 17 stage였고 현재는 18개"라는 주석을 추가해 헷갈리지 않게 함(과거 기록 자체는 지우지 않음, 히스토리 보존 원칙 유지). `SKILL.md`/`실행방법.md`/`최종요약보고서`는 전부 이미 18로 일관되게 써 있었음(추가 수정 불필요, grep으로 확인).
- **`실행방법.md`의 스스로 모순되는 부분 발견 및 수정**: 1단계에서 사업자 정보 필드를 실제로 연결했는데, `실행방법.md` 1부에는 여전히 "이 값은 아직 시스템이 자동으로 채워주지 않고, 사람이 직접 입력하는 칸"이라는 옛 문구가 남아있었음 — "이제 현장완료 입력 때 한 번 넣으면 문서 생성 시 자동으로 불러온다"로 수정. 2부의 "이번 전체 점검에서 발견된 사항 3건"도 전부 [해결] 표시로 갱신.
- **`최종요약보고서_2026-07-07.md`의 같은 모순 발견 및 수정**: "사업자 정보 항목은 시스템이 자동으로 채워주지 않으므로"라는 문구를 "이제 시스템이 저장해뒀다가 자동으로 불러와 채운다(단, 값이 맞는지 확인은 여전히 사람 몫)"로 수정.
- 카드 개수(5개)·테스트 건수(12건) 등 다른 숫자는 4개 문서 전부 grep으로 재확인 — 불일치 없음.

### 4단계 — `방역스킬_현황보고.md` 신규 작성 (`1. Projects/클로드 방역 ai/방역스킬_현황보고.md`)
맨 위에 개발 용어 없이 "지금 대표님이 하실 일은 이것 하나입니다"로 시작하는 문단 추가 — golden_set_template.yaml을 채워서 보내주는 것이 유일한 액션임을 강조. 그 아래 쉬운 말 진행상황 요약, 아직 결정할 5가지, 아직 조심할 것, 참고 파일 표를 담음.

### 이번 회차에서 수정/신규 작성한 파일
- 수정: `manifest.yaml`(business_info 필드+연결), `scripts/run.py`(business_info 저장/전달 구현), `SKILL.md`(실행방법/알려진한계 재작성), `templates/*.md` 3종(작성상태 표기 통일), `golden_set_template.yaml`(필드 3개 추가), `실행방법.md`(사업자정보 문구 정정 + 발견사항 3건 [해결] 갱신), `최종요약보고서_2026-07-07.md`(사업자정보 문구 정정)
- 신규: `1. Projects/클로드 방역 ai/방역스킬_현황보고.md`

### 구조 변경 없음 확인
이번 회차 전부 "필드 추가"와 "문서 말맞추기" 범위 안에서 처리함 — stage 그래프, depends_on, 새 stage 종류 등 구조적인 것은 하나도 안 바꿈(사용자의 사전 전제와 일치). 구조변경 후보 카드는 여전히 5개, 사용자 판단 대기 그대로.

---

## 2026-07-07 (같은 날, 5번째 세션) — 재검증만 수행 (신규 변경 없음, 카드 표는 채팅으로 직접 제시)

**배경**: 사용자가 다시 "1~4단계"(필드 6개 반영 → 갭 3개 재분류 → 최종 카드 정리 → HANDOFF 갱신)를 지시. 파일을 확인해보니 4단계 전부 이전 세션들에서 이미 반영·해결·정리까지 끝나 있었음. 이번 세션은 새로 만들거나 바꾼 것 없이 **재검증만** 수행함:

- `validate_manifest.py "1. Projects/클로드 방역 ai/manifest.yaml"` 재실행 → FAIL 0 / WARN 0.
- `test_12cases_batch.py` 재실행 → 검증1(문서종류구분)/검증2(승인워크플로우 A·B·C)/검증3(entry_points+날짜계산) 전부 "[해결됨]", 전체 결과 PASS.
- 최종 카드 5개(card-01/02/05/10/11) 표를 사용자에게 채팅으로 직접 제시(사용자가 "내가 직접 보고 판단할 것"이라고 명시해 파일이 아니라 대화창에 표로 보여줌).
- 위쪽 상태 줄("- 상태:")의 "Fable 카드 11개"라는 오래된 표현이 안 고쳐진 채 남아있던 것을 발견해 "남은 구조변경 카드 5개"로 정정(표 자체 내용은 9번째 줄·41번째 줄에 이미 5개로 반영돼 있었고, 이 상태 요약 줄 하나만 안 고쳐져 있었음).
- HANDOFF.md 자체도 이번 편집 중 truncation이 재현돼 `.stale-*`로 rename 후 Write로 전체 재작성해 복구함(반복되는 known issue).

**구조/필드 변경 없음** — 이번 세션은 순수하게 확인·재검증·표 제시만 수행함.

---

## 관련 문서

- [[1. Projects/클로드 방역 ai/방역스킬_현황보고|방역스킬_현황보고]]
- [[1. Projects/클로드 방역 ai/실행방법|실행방법]]
- [[1. Projects/클로드 방역 ai/3단계_12건테스트_결과요약|3단계_12건테스트_결과요약]]
- [[1. Projects/클로드 방역 ai/최종요약보고서_2026-07-07|최종요약보고서_2026-07-07]]
- [[ai공장짓기/방역_업무흐름_및_꽃집재사용성_분석_2026-07-07|방역_업무흐름_및_꽃집재사용성_분석]]
- [[1. Projects/클로드 방역 ai/SKILL|SKILL]]
- [[ai공장짓기/decision-log_skill-factory-architecture|decision-log_skill-factory-architecture]]

<!-- ok -->
