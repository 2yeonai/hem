# TEST_REPORT.md — 앱 MVP 테스트 리포트

테스트일: 2026-07-03 / 환경: Linux, Python 3.x, 외부 의존성 없음

## 1. 엔진 단위 테스트 (test_engines.py)

```bash
python3 08_factory_tools/test_engines.py
→ 결과: 29/29 통과
```

검증된 것: raw_text 유무에 따른 [확인 필요] 모드 / 체납=있음 → BLOCKED / 위험 표현("완벽히 해결") 검출·대체 제안 / idea1 추천·idea3 탈락 판정 / 장비 구매 플래그·견적 필요 표시 / **승인을 다 해도 위험 표현·원문 부재 시 LOCK 불가** / 원문 없으면 "제출 불가" 고정 / Q&A 10문항·예산 질문 발화 금지 / 재작업 1순위에 위험 표현 포함, 탈락 후보 혼입 금지.

발견·수정된 버그 1건: 원문에 "제출서류 부분 미확보"라고 쓴 줄을 추출기가 실제 정보로 오인 → "미확보/유실" 언급 줄 제외 처리 후 통과.

## 2. 샘플 프로젝트 실행 (sample-demo, 가상 공고)

입력: 가상 소상공인 디지털 전환 공고(원문 일부 유실 상황 재현), 신청자(중복수혜 미확인), 아이디어 3개(idea3에 위험 표현 2종 포함), 예산 3항목 중 2항목 금액 미확보, 증빙 일부만 확보.

| 기대 결과 | 실제 결과 |
|---|---|
| 원문 일부 부족 → [확인 필요] 표시 | ✅ 제출서류·접수마감 등 [확인 필요] 목록 생성 |
| 아이디어 비교표 생성 | ✅ 6축 채점, idea1 42점대 추천 |
| 최종 추천 생성 | ✅ idea1 추천, idea2·idea3 탈락 후보 → 혼입 금지 목록 |
| 실측·견적 부족 시 LOCK은 DRAFT 유지 | ✅ 승인 후에도 DRAFT (위험 표현 1건 + 예산 미확정 잔존) |
| 위험 표현 검출 | ✅ "완벽히 해결" 검출 + 대체 표현 제안 |
| 예상 점수 생성 | ✅ 81/100, 판정 "보완 후 가능" (80점 이상이어도 위험 표현·견적 문제로 '제출 가능' 아님) |
| revision_order 생성 | ✅ 1순위 4건 (중복수혜 확인, 위험 표현 2건 수정, 견적서) |
| HANDOFF 생성 | ✅ 10_handoff/HANDOFF_sample-demo.md |

생성 파일: 14개 (04_outputs 6 + 05_reviews 7 + HANDOFF 1)

## 3. 웹앱 화면 테스트 (curl 기반)

| 검증 | 결과 |
|---|---|
| 대시보드(/) 렌더링·프로젝트 목록 | ✅ 200 |
| 샘플 실행 버튼(POST /run_sample) → 리다이렉트 | ✅ 303 → /project/sample-demo |
| 8개 탭 전부 렌더링 | ✅ 전부 200 |
| 위험 배너·위험 표현 화면 노출 | ✅ "위험 표현 1건", "완벽히 해결" 표시 |
| 승인 POST → input.json 기록 → 재분석 | ✅ selected=idea1, approvals 저장, LOCK은 DRAFT 유지(정상) |
| 새 프로젝트 폼(/new) | ✅ 200 |
| 파일 열람(/file/...results.json) | ✅ 200 |
| 경로 탈출(/file/../etc/passwd) 차단 | ✅ 404 |

## 4. CLI 스모크 테스트 (별도 경로, run_factory.py)

빈 입력 실행 → PENDING_APPROVAL 정지(exit 4) + 정지 보고서 생성 ✅ / 승인 후 7단계 완주(exit 0) ✅ / 체납=true → BLOCKED(exit 3) ✅ / 위험 표현 스캔이 29개 항목 대조로 검출 ✅

## 5. 미테스트 영역 (정직 고지)

- Windows·macOS 실행 (Linux만 검증) [확인 필요]
- 브라우저 실사용(폼 UX) — curl 검증까지만, 사람 클릭 테스트는 사용자 몫
- 대용량 공고문(수백 KB) 성능
- 실제 공고 데이터 (전부 가상 샘플)

<!-- ok -->
